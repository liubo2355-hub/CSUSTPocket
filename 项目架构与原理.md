# 掌上长理（CSUSTPocket）项目架构与原理

> 本文档基于 workspace 源码逐文件整理，所有内容都引用具体文件路径与行号，可点击跳转。代码片段与注释均原样保留。

---

## 目录

1. [项目概述](#1-项目概述)
2. [工程结构与模块](#2-工程结构与模块)
3. [构建配置与依赖栈](#3-构建配置与依赖栈)
4. [应用启动链路](#4-应用启动链路)
5. [核心架构：MVI + Store + Repository](#5-核心架构mvi--store--repository)
6. [网络层 OkHttpHelper](#6-网络层-okhttphelper)
7. [数据层：MMKV / Room / 三级缓存](#7-数据层mmkv--room--三级缓存)
8. [首页 Overview 模块详解](#8-首页-overview-模块详解)
9. [考试高亮选择器 ExamHighlightSelector](#9-考试高亮选择器-examhighlightselector)
10. [电费查询模块](#10-电费查询模块)
11. [成绩查询与 GPA 计算](#11-成绩查询与-gpa-计算)
12. [课表模块](#12-课表模块)
13. [慕课 MOOC 模块](#13-慕课-mooc-模块)
14. [校历与学期周次计算](#14-校历与学期周次计算)
15. [公告系统](#15-公告系统)
16. [更新检查与下载安装](#16-更新检查与下载安装)
17. [主题与换肤系统](#17-主题与换肤系统)
18. [账号体系与登录绑定](#18-账号体系与登录绑定)
19. [路由系统](#19-路由系统)
20. [跨模块数据流转路径汇总](#20-跨模块数据流转路径汇总)
21. [文件行号速查表](#21-文件行号速查表)
22. [当前使用状态总览](#22-当前使用状态总览)
23. [阅读建议](#23-阅读建议)

---

## 1. 项目概述

**掌上长理**（应用名 CSUSTPocket，包名 `com.creamaker.pocket_csust`）是面向长沙理工大学学生的校园工具类 Android 应用。当前版本：

- `versionName = "2.0.32"`，`versionCode = 54`
- 见 [app/build.gradle.kts:42-43](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L42-L43)

技术栈：Kotlin + Jetpack Compose + Material3 + Coroutines/Flow + MMKV + Room + OkHttp + csust_spider aar。

架构新旧并存：
- 新页面：Compose + MVVM/MVI + StateFlow
- 旧页面：View + XML + RxJava + Store 模式

---

## 2. 工程结构与模块

### 2.1 顶层结构

```
CSUSTPocket/
├── app/                      # 主模块
├── CP_Common/                # 公共库模块
├── baselineprofile/          # Baseline Profile 生成
├── build.gradle.kts
├── settings.gradle.kts
└── 项目架构与原理.md          # 本文档
```

### 2.2 app 模块源码包结构

```
com.creamaker.changli_planet_app
├── core/                     # 应用核心
│   ├── PlanetApplication.kt          # Application
│   ├── MainActivity.kt               # 主 Activity
│   ├── Route.kt                      # 路由
│   ├── Store.kt                      # Redux 基类
│   ├── network/                      # 网络层
│   ├── mvi/                          # MVI 基类
│   ├── main/                         # 主屏
│   ├── theme/                        # 主题
│   └── designsystem/                 # Hyper 设计系统
├── overview/                 # 首页模块
│   ├── data/                         # Repository/选择器/缓存
│   ├── announcement/                 # 公告模块
│   ├── ui/                           # Fragment/Compose UI/Model
│   └── viewmodel/                    # ViewModel
├── feature/                  # 业务功能
│   ├── common/                       # 电费/成绩/通用
│   ├── timetable/                    # 课表
│   ├── calendar/                     # 校历
│   ├── mooc/                         # 慕课
│   ├── physics/                      # 物理实验
│   └── ledger/                       # 账本
├── common/                   # 全局公共代码
│   ├── update/                       # 更新检查
│   ├── service/                      # 下载服务
│   ├── redux/                        # UserStore/UserAction
│   ├── cache/CommonInfo.kt
│   └── data/remote/dto/              # 更新 DTO
├── profileSettings/          # 个人设置
├── freshNews/                # 新鲜事
├── skin/                     # 换肤系统
├── widget/                   # 自定义 View
└── utils/                    # 工具类
```

---

## 3. 构建配置与依赖栈

### 3.1 Android 配置块

[app/build.gradle.kts:31-148](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L31-L148)：

```kotlin
android {
    namespace = "com.creamaker.changli_planet_app"
    compileSdk = 36
    compileSdkMinor = 1
    buildFeatures { buildConfig = true; compose = true }

    defaultConfig {
        applicationId = "com.creamaker.pocket_csust"
        minSdk = 24
        targetSdk = 36
        versionCode = 54
        versionName = "2.0.32"
        ndk { abiFilters.add("arm64-v8a") }
    }
}
```

### 3.2 三套构建类型

[app/build.gradle.kts:84-121](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L84-L121)：

- `debug`：开发环境，走 `api-dev.planet.zhelearn.com`
- `release`：线上环境，走 `api.planet.zhelearn.com`，开启混淆 + 资源压缩
- `benchmark`：贴近线上环境，用于生成 Baseline Profile

BaseUrl 三级兜底（local.properties → 环境变量 → 默认值），见 [app/build.gradle.kts:24-29](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L24-L29)：

```kotlin
val planetApiBaseUrlDebug: String = localProperties.getProperty("planet.apiBaseUrl.debug")
    ?: System.getenv("PLANET_API_BASE_URL_DEBUG")
    ?: "https://api-dev.planet.zhelearn.com/v1/"
```

### 3.3 APK 输出文件名

[app/build.gradle.kts:150-158](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L150-L158)：

```kotlin
androidComponents {
    onVariants { variant ->
        variant.outputs.forEach { output ->
            val versionName = output.versionName.orNull ?: ""
            (output as? VariantOutputImpl)
                ?.outputFileName?.set("CSUSTPocket_${variant.name}_v${versionName}.apk")
        }
    }
}
```

### 3.4 关键依赖

[app/build.gradle.kts:159-259](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L159-L259)：

| 类别 | 依赖 |
|------|------|
| UI | Compose BOM、Material3、Navigation3、Haze（毛玻璃）、ConstraintLayout-Compose |
| 异步 | Coroutines、RxJava3/RxAndroid |
| 网络 | OkHttp、Retrofit、Gson、Jsoup、腾讯云 HTTPDNS |
| 存储 | Room + KSP、MMKV |
| 图片 | Glide、Coil、PhotoView、uCrop、SubsamplingScaleImageView |
| 地图 | `com.amap.api:3dmap-location-search:11.2.000...`（高德 3D 矢量地图） |
| 崩溃 | 腾讯 Bugly |
| 其他 | Lottie、WheelPicker、TimetableView、Flexbox、WorkManager |
| 项目内 | `implementation(project(":CP_Common"))`、`implementation(libs.csustdataget)` |
| Profile | `"baselineProfile"(project(":baselineprofile"))` |

---

## 4. 应用启动链路

### 4.1 PlanetApplication.onCreate

[core/PlanetApplication.kt:159-191](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L159-L191)：

```kotlin
override fun onCreate() {
    super.onCreate()
    appContext = applicationContext
    initMMKV()
    PlanetApplication.deviceId = getSystemDeviceId()
    ThemeModeManager.initialize()
    migrateLegacyCacheIfNeeded()
    if (!BuildConfig.DEBUG) {
        CrashReport.initCrashReport(applicationContext, "1c79201ce5", true)
    }
    if (BuildConfig.DEBUG) {
        StartupTimeTracker.initialize(applicationContext as Application)
    }
    applicationScope.launch {
        runCatching { initDNS() }.onFailure { Log.e("DNS", "DNS, Error") }
        runCatching { preRequestIps.forEach { OkHttpHelper.preRequest(it) } }
    }
    setSkin()
    saveDefaultSkin()
    initAMapPrivacy()
}
```

启动流程：
1. `initMMKV()`：MMKV 初始化
2. `getSystemDeviceId()`：ANDROID_ID 派生 deviceId
3. `ThemeModeManager.initialize()`：主题模式恢复
4. `migrateLegacyCacheIfNeeded()`：缓存 schema 迁移
5. release 模式初始化 Bugly，debug 模式初始化启动时间追踪
6. 异步并行：HTTPDNS + 预热连接
7. `setSkin()`：恢复上次皮肤
8. `initAMapPrivacy()`：高德隐私合规

### 4.2 全局字段

[core/PlanetApplication.kt:34-66](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L34-L66)：

```kotlin
companion object {
    var accessToken: String?
        get() = MMKV.defaultMMKV().getString("token", null)
        set(value) { MMKV.defaultMMKV().putString("token", value) }

    var isExpired: Boolean
        get() = MMKV.defaultMMKV().getBoolean("is_expired", true)
        set(value) { MMKV.defaultMMKV().putBoolean("is_expired", value) }

    const val UserIp: String = "https://user.csust.creamaker.cn"
    const val ToolIp: String = "https://web.csust.creamaker.cn"

    val preRequestIps = listOf(
        "https://user.csust.creamaker.cn",
        "https://web.csust.creamaker.cn",
    )
}
```

### 4.3 缓存迁移 migrateLegacyCacheIfNeeded

[core/PlanetApplication.kt:249-272](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L249-L272)：

```kotlin
private fun migrateLegacyCacheIfNeeded() {
    val kv = MMKV.defaultMMKV() ?: return
    val version = kv.decodeInt(CACHE_SCHEMA_VERSION_KEY, 0)
    if (version >= CURRENT_CACHE_SCHEMA_VERSION) return  // 当前 v4

    runCatching {
        if (version < 3) {
            MMKV.mmkvWithID("education_cache").clearAll()
            MMKV.mmkvWithID("content_cache").removeValueForKey("grades")
            MMKV.mmkvWithID("content_cache").removeValueForKey("exams")
            MMKV.mmkvWithID("MoocCookiejar").clearAll()
        }
        if (version < 4) {
            MMKV.mmkvWithID("import_cache").apply {
                removeValueForKey("user_account")
                removeValueForKey("user_avatar")
                removeValueForKey("user_id")
                removeValueForKey("user_email")
            }
        }
    }
    kv.encode(CACHE_SCHEMA_VERSION_KEY, CURRENT_CACHE_SCHEMA_VERSION)
}
```

### 4.4 MainActivity.onCreate

[core/MainActivity.kt:34-76](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt#L34-L76)：

```kotlin
class MainActivity : ComponentActivity(), DrawerController {
    private val store by lazy { UserStore() }
    private val mainTabNavigator by lazy(LazyThreadSafetyMode.NONE) { MainTabNavigator() }

    override fun onResume() {
        super.onResume()
        store.dispatch(UserAction.initilaize())
        AppUpdateChecker.check(this)
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        setCustomDensity(this, application, 412)
        super.onCreate(savedInstanceState)
        lifecycleScope.launch {
            repeatOnLifecycle(Lifecycle.State.STARTED) {
                AppEventBus.selectEvent.collect { event ->
                    mainTabNavigator.select(event.eventType)
                }
            }
        }
        CommonInfo.startTime = System.currentTimeMillis()
        enableEdgeToEdge()
        setContent {
            AppSkinTheme {
                Surface(color = AppTheme.colors.bgPrimaryColor) {
                    MainScreen(navigator = mainTabNavigator)
                }
            }
        }
        Looper.myQueue().addIdleHandler {
            getNetPermissions()
            false
        }
    }
}
```

### 4.5 onStart 静默刷新 MOOC 资料

[core/MainActivity.kt:101-109](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt#L101-L109)：

```kotlin
override fun onStart() {
    super.onStart()
    // 账号系统已取消，不再拉取自研后端的用户资料。
    // 但从老版本升级上来时，MMKV 中原本由自研后端写入的头像/昵称已被
    // migrateLegacyCacheIfNeeded 清除，这里用已绑定的学号/密码做一次静默 SSO
    // 重新登录 + getLoginUser，把 MOOC(网络教学平台) 的用户名/头像写回 UserInfoManager。
    store.dispatch(UserAction.RefreshMoocProfileSilently)
}
```

### 4.6 屏幕适配 setCustomDensity

[core/MainActivity.kt:147-173](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt#L147-L173)：

```kotlin
private fun setCustomDensity(activity: Activity, application: Application, designWidthDp: Int) {
    val appDisplayMetrics = application.resources.displayMetrics
    val targetDensity = appDisplayMetrics.widthPixels / designWidthDp.toFloat()
    val targetDensityDpi = (targetDensity * 160).toInt()
    var nonCompatScaleDensity = appDisplayMetrics.scaledDensity
    // 注册 ComponentCallbacks 监听字体变化，避免 scaledDensity 失同步
    val targetScaleDensity = targetDensity * (nonCompatScaleDensity / appDisplayMetrics.density)
    appDisplayMetrics.density = targetDensity
    appDisplayMetrics.densityDpi = targetDensityDpi
    appDisplayMetrics.scaledDensity = targetScaleDensity
    activity.resources.displayMetrics.density = targetDensity
    activity.resources.displayMetrics.densityDpi = targetDensityDpi
    activity.resources.displayMetrics.scaledDensity = targetScaleDensity
}
```

设计宽度 412dp，类似今日头条屏幕适配方案。

---

## 5. 核心架构：MVI + Store + Repository

应用存在两套并行的状态管理架构。

### 5.1 旧页面：Store + RxJava

[core/Store.kt:9-26](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Store.kt#L9-L26)：

```kotlin
/**
 * 所有调度器的基类，所有Store都应该继承Store基类
 */
open abstract class Store<S, A> {
    private val eventStream = PublishSubject.create<A>()
    val _state = PublishSubject.create<S>()

    init {
        eventStream.observeOn(AndroidSchedulers.mainThread())
            .subscribe { action ->
                handleEvent(action)
            }
    }

    abstract fun handleEvent(action: A)
    fun dispatch(action: A & Any) {
        eventStream.onNext(action)
    }

    fun state() = _state.observeOn(AndroidSchedulers.mainThread())
}
```

`UserStore` 是现存最重要的 Store 子类（[common/redux/store/UserStore.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt)），负责更新检查、学号绑定、静默刷新 MOOC 资料。

### 5.2 新页面：MviViewModel + StateFlow

[core/mvi/MviViewModel.kt:9-26](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/mvi/MviViewModel.kt#L9-L26)：

```kotlin
abstract class MviViewModel<I : MviIntent, S : MviViewState> : ViewModel() {

    private val _state = MutableStateFlow<S>(initialState())
    val state: StateFlow<S> = _state.asStateFlow()

    abstract fun processIntent(intent: I)
    abstract fun initialState(): S

    protected fun updateState(reducer: S.() -> S) {
        _state.update { it.reducer() }
    }
}
```

### 5.3 新页面：MVVM + StateFlow（无 MVI）

首页 `OverviewViewModel` 不继承 `MviViewModel`，直接用 `MutableStateFlow<OverviewUiState>`，通过 `fun refreshSection(key: String)` 等方法触发刷新，而不是 `processIntent`。这是更轻量的 MVVM 形式。

---

## 6. 网络层 OkHttpHelper

[core/network/OkHttpHelper.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt) 是全局 OkHttp 管理类，477 行。

### 6.1 AuthInterceptor 鉴权流程

[core/network/OkHttpHelper.kt:40-120](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt#L40-L120)：

```kotlin
class AuthInterceptor(private val tokenExpiredHandler: TokenExpiredHandler? = null) : Interceptor {
    companion object {
        private const val MAX_AUTH_RESPONSE_PEEK_BYTES = 64L * 1024L
        private val tokenRefreshLock = Any()
    }

    override fun intercept(chain: Interceptor.Chain): Response {
        val originalRequest = chain.request()
        if (PlanetApplication.isExpired) {
            return chain.proceed(originalRequest)
        }
        val requestToken = PlanetApplication.accessToken.orEmpty()
        val requestWithToken = originalRequest.newBuilder()
            .header("Authorization", requestToken)
            .header("deviceId", PlanetApplication.deviceId)
            .build()
        // 跳过 /app/users/me/token PUT 请求，避免递归刷新
        val response = chain.proceed(requestWithToken)
        // 仅 JSON 响应才进行 401 检测（文件流不可 peekBody）
        val realResponse = peekResponse(response)
        if (realResponse?.code == "401" && realResponse.msg == PlanetConst.UNAUTHORIZATION) {
            val newToken = synchronized(tokenRefreshLock) {
                val latestToken = PlanetApplication.accessToken.orEmpty()
                if (latestToken.isNotBlank() && latestToken != requestToken) {
                    latestToken  // 复用其他并发请求已刷新的 token
                } else {
                    refreshTokenSync()
                }
            }
            if (!newToken.isNullOrBlank()) {
                response.close()
                val newRequest = originalRequest.newBuilder()
                    .header("Authorization", newToken)
                    .header("deviceId", PlanetApplication.deviceId)
                    .build()
                return chain.proceed(newRequest)
            }
            PlanetApplication.clearLocalCache()
            tokenExpiredHandler?.onTokenExpired()
        }
        return response
    }
}
```

关键点：
- 64KB peekBody 避免 JSON 大响应内存爆炸
- `tokenRefreshLock` 全局锁，避免并发请求同时刷 token
- 复用机制：如果 `latestToken != requestToken`，说明其他请求已刷新过，直接用新 token 重发
- 刷新失败：清空本地缓存 + 通知主线程设置 `isExpired = true`

### 6.2 HTTPDNS 集成

[core/network/OkHttpHelper.kt:124-184](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt#L124-L184)：

```kotlin
private val client: OkHttpClient by lazy {
    OkHttpClient.Builder()
        .dns(object : Dns {
            override fun lookup(hostname: String): List<InetAddress> {
                val sanitizedHost = DnsSafety.sanitizeHostname(hostname)
                if (sanitizedHost.isBlank()) return emptyList()
                return try {
                    if (!DnsSafety.shouldUseHttpDns(sanitizedHost)) {
                        return DnsSafety.fallbackToLocalDns(sanitizedHost)
                    }
                    val ips = MSDKDnsResolver.getInstance().getAddrByName(sanitizedHost)
                    val ipArr = DnsSafety.parseIpList(ips)
                    if (ipArr.isEmpty()) {
                        DnsSafety.fallbackToLocalDns(sanitizedHost)
                    } else { /* 构造 InetAddress 列表 */ }
                } catch (e: Exception) {
                    DnsSafety.fallbackToLocalDns(sanitizedHost)
                }
            }
        })
        .connectTimeout(10, TimeUnit.SECONDS)
        .readTimeout(10, TimeUnit.SECONDS)
        .writeTimeout(10, TimeUnit.SECONDS)
        .addInterceptor(NetworkLogger.getLoggingInterceptor())
        .addInterceptor(AuthInterceptor(object : AuthInterceptor.TokenExpiredHandler {
            override fun onTokenExpired() {
                Handler(Looper.getMainLooper()).post {
                    PlanetApplication.isExpired = true
                }
            }
        }))
        .build()
}
```

降级路径：HTTPDNS → LocalDNS，任何异常都 fallback 到 `DnsSafety.fallbackToLocalDns`。

### 6.3 Token 刷新 refreshTokenSync

[core/network/OkHttpHelper.kt:252-282](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt#L252-L282)：

```kotlin
private fun refreshTokenSync(): String? {
    try {
        val request = Request.Builder()
            .url("${PlanetApplication.UserIp}/me/token")
            .addHeader("Authorization", PlanetApplication.accessToken ?: "")
            .addHeader("deviceId", PlanetApplication.deviceId)
            .put("".toRequestBody("application/json; charset=utf-8".toMediaTypeOrNull()))
            .build()
        return client.newCall(request).execute().use { response ->
            when {
                response.isSuccessful -> {
                    response.headers["Authorization"]?.also { newToken ->
                        PlanetApplication.accessToken = newToken
                    }
                }
                else -> { Log.w("Token Refresh", "Failed to refresh token: ${response.code}") }
            }
            response.headers["Authorization"]
        }
    } catch (e: Exception) {
        Log.e("Token Refresh", "Network error during token refresh", e)
        return null
    }
}
```

### 6.4 预连接优化

[core/network/OkHttpHelper.kt:454-476](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt#L454-L476)：

```kotlin
/**
 * 利用OkHttp的连接复用池，预连接以提高网络请求效率
 * 初步构想闪屏页面预请求首页数据
 */
fun preRequest(url: String) {
    val request = Request.Builder().url(url).head().build()
    client.newCall(request).enqueue(object : Callback {
        override fun onResponse(call: Call, response: Response) { }
        override fun onFailure(call: Call, e: IOException) { }
    })
}
```

---

## 7. 数据层：MMKV / Room / 三级缓存

### 7.1 MMKV 缓存分区

| 缓存 ID | 职责 | 管理者 |
|---------|------|--------|
| 默认实例 | token、is_expired、deviceId | PlanetApplication |
| `stu_info_cache` | 学号、密码 | StudentInfoManager |
| `import_cache` | MOOC userId、account、avatar、email | UserInfoManager |
| `education_cache` | 教务相关 | — |
| `content_cache` | grades、exams | ScoreCache、ExamArrangementCache |
| `overview_local_cache` | 电量快照、历史 | OverviewLocalCache |
| `mooc_local_cache` | 慕课课程 | MoocLocalCache |
| `MoocCookiejar` | MOOC 登录 Cookie | — |
| `announcement_cache` | 公告清单、ETag、已读 | AnnouncementRepository |
| `TimeTableAppWidget` | 课表小组件 | — |

### 7.2 StudentInfoManager

[common/data/local/mmkv/StudentInfoManager.kt:5-48](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/local/mmkv/StudentInfoManager.kt#L5-L48)：

```kotlin
object StudentInfoManager {
    private const val CACHE_ID = "stu_info_cache"
    private const val LEGACY_CACHE_ID = "import_cache"
    private const val KEY_STUDENT_ID = "student_id"
    private const val KEY_PASSWORD = "student_password"

    private val mmkv by lazy {
        MMKV.mmkvWithID(CACHE_ID).also { target -> migrateFromLegacyIfNeeded(target) }
    }

    var studentId: String
        get() = mmkv.getString(KEY_STUDENT_ID, "") ?: ""
        set(value) { mmkv.putString(KEY_STUDENT_ID, value) }

    var studentPassword: String
        get() = mmkv.getString(KEY_PASSWORD, "") ?: ""
        set(value) { mmkv.putString(KEY_PASSWORD, value) }

    fun clear() {
        studentId = ""
        studentPassword = ""
    }
}
```

启动时自动从 legacy `import_cache` 迁移学号/密码。

### 7.3 UserInfoManager

[common/data/local/mmkv/UserInfoManager.kt:60-87](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/local/mmkv/UserInfoManager.kt#L60-L87)：

```kotlin
/** Clears every account-bound profile field when the user explicitly signs out. */
fun clearBoundAccount() {
    mmkv.removeValuesForKeys(arrayOf(KEY_USERID, KEY_USERNAME, KEY_USER_PASSWORD, KEY_AVATAR, KEY_USER_ACCOUNT, KEY_EMAIL))
    PlanetApplication.accessToken = null
}

/**
 * 切换学号成功后的清理：仅移除"不会被新登录覆盖"的旧账号残留字段（邮箱 / userId）。
 *
 * 注意：
 * - username / account / userPassword —— 已由 [UserAction.BindingStudentNumber] 的 SSO 成功分支
 *   写入为新账号的值，这里不要清。
 * - userAvatar —— 同样会在 SSO 成功分支被写为新账号头像，因此不能在此清除，
 *   否则 UI 会拿回默认占位头像，造成"切换账号后头像丢失"的回归。
 */
fun clearStaleProfile() {
    mmkv.removeValueForKey(KEY_EMAIL)
    mmkv.removeValueForKey(KEY_USERID)
}
```

### 7.4 Room：CoursesDataBase

[feature/common/data/local/room/database/CoursesDataBase.kt:12-30](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/room/database/CoursesDataBase.kt#L12-L30)：

```kotlin
@Database(entities = [TimeTableMySubject::class], version = 10, exportSchema = true)
@TypeConverters(WeeksTypeConverter::class)
abstract class CoursesDataBase : RoomDatabase() {
    abstract fun courseDao(): CourseDao

    companion object {
        @Volatile
        private var INSTANCE: CoursesDataBase? = null
        fun getDatabase(context: Context) = INSTANCE ?: synchronized(this) {
            val instance = Room.databaseBuilder(
                context.applicationContext,
                CoursesDataBase::class.java,
                "course_database"
            ).build()
            INSTANCE = instance
            instance
        }
    }
}
```

### 7.5 TimeTableMySubject 实体

[feature/common/data/local/entity/TimeTableMySubject.kt:13-36](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/entity/TimeTableMySubject.kt#L13-L36)：

```kotlin
@Entity(
    tableName = "courses",
    indices = [Index(
        value = ["courseName", "classroom", "teacher", "start", "step", "weekday", "term"],
        unique = true
    )]
)
@Keep
data class TimeTableMySubject(
    var courseName: String = "",
    var classroom: String? = "",
    var teacher: String = "",
    @TypeConverters(WeeksTypeConverter::class) var weeks: List<Int>? = emptyList(),
    var start: Int = 0,
    var step: Int = 0,
    var weekday: Int = 0,
    @PrimaryKey(autoGenerate = true) var id: Int = 0,
    val isCustom: Boolean = false,
    var term: String = "",
    val studentId: String = "",
    val studentPassword: String = ""
) : ScheduleEnable
```

唯一索引确保同一学期内同课程+教室+教师+节次不会重复入库。

### 7.6 三级缓存：SemesterCalendarRepository

[feature/calendar/data/repository/SemesterCalendarRepository.kt:37-98](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/calendar/data/repository/SemesterCalendarRepository.kt#L37-L98)：

```kotlin
object SemesterCalendarRepository {
    @Volatile private var memoryList: List<SemesterCalendarListItem>? = null
    private val memoryDetails = ConcurrentHashMap<String, SemesterCalendarDetail>()

    /** 同步获取校历列表：仅读内存/本地，不触发网络。 */
    fun getListSync(): List<SemesterCalendarListItem>? {
        memoryList?.let { return it }
        return SemesterCalendarCache.getList()?.also { memoryList = it }
    }

    suspend fun getList(forceRefresh: Boolean = false): Result<List<SemesterCalendarListItem>> =
        withContext(Dispatchers.IO) {
            if (!forceRefresh) {
                getListSync()?.let { return@withContext Result.success(it) }
            }
            val remote = runCatching { api.getSemesterCalendars() }
            remote.onSuccess { list ->
                memoryList = list
                SemesterCalendarCache.saveList(list)
            }
            // 非强制刷新场景下，网络失败 + 有本地缓存 → 降级返回 success
            if (remote.isFailure && !forceRefresh) {
                getListSync()?.let { return@withContext Result.success(it) }
            }
            remote
        }
}
```

[feature/calendar/data/repository/SemesterCalendarRepository.kt:163-169](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/calendar/data/repository/SemesterCalendarRepository.kt#L163-L169)：

```kotlin
/**
 * 静默触发一次学期详情刷新，**fire-and-forget 语义**：
 *  - 立即返回，不阻塞调用方 coroutineScope；
 *  - 仅当本地无缓存时才真正发起网络请求；
 *  - 失败吞掉。
 */
fun prefetchDetailIfMissing(semesterCode: String) {
    if (semesterCode.isBlank()) return
    if (getDetailSync(semesterCode) != null) return
    backgroundScope.launch {
        runCatching { getDetail(semesterCode, forceRefresh = false) }
    }
}
```

---

## 8. 首页 Overview 模块详解

### 8.1 入口

- [OverviewFragment.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/OverviewFragment.kt)：Fragment 容器
- [OverviewScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt)：Compose UI（2212 行）
- [OverviewViewModel.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt)：状态持有者
- [OverviewRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt)：数据聚合（555 行）

### 8.2 OverviewRepository 数据聚合

[overview/data/OverviewRepository.kt:44-83](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L44-L83)：

```kotlin
class OverviewRepository(context: Context) {
    private val appContext = context.applicationContext
    private val courseDao by lazy { CoursesDataBase.getDatabase(appContext).courseDao() }
    private val userDao by lazy { UserDataBase.getInstance(appContext).itemDao() }
    private val examCache by lazy { ExamArrangementCache() }
    private val electricityRepository by lazy { ElectricityRepository() }

    suspend fun loadLocalState(): OverviewUiState = withContext(Dispatchers.IO) {
        buildState(
            courses = readLocalCourses(),
            grades = ScoreCache.getGrades().orEmpty(),
            exams = examCache.getExamArrangement().orEmpty().toUiExams()
        )
    }

    suspend fun refreshState(): OverviewUiState = withContext(Dispatchers.IO) {
        coroutineScope {
            val currentTerm = getCurrentTerm()
            // 刷新前触发一次校历预取（非阻塞 fire-and-forget，失败静默）
            SemesterCalendarRepository.prefetchDetailIfMissing(currentTerm)
            val courseDeferred = async(Dispatchers.IO) { fetchCourses(currentTerm) }
            val gradesDeferred = async(Dispatchers.IO) { fetchGrades() }
            val examsDeferred = async(Dispatchers.IO) { fetchExams(currentTerm) }
            val electricityDeferred = async(Dispatchers.IO) { refreshElectricityIfNeeded() }
            val courses = courseDeferred.await() ?: readLocalCourses()
            val grades = gradesDeferred.await() ?: ScoreCache.getGrades().orEmpty()
            val exams = examsDeferred.await() ?: examCache.getExamArrangement().orEmpty().toUiExams()
            electricityDeferred.await()

            buildState(courses = courses, grades = grades, exams = exams).copy(isSilentSyncing = false)
        }
    }
}
```

`refreshState()` 用 `coroutineScope + async` 并行拉取 4 个数据源，任何失败都回退到本地缓存。

### 8.3 buildState 21 点切明日

[overview/data/OverviewRepository.kt:98-177](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L98-L177)：

```kotlin
private suspend fun buildState(
    courses: List<TimeTableMySubject>,
    grades: List<Grade>,
    exams: List<OverviewExamUiModel>
): OverviewUiState = withContext(Dispatchers.IO) {
    // ...
    val isShowingTomorrow = Calendar.getInstance().get(Calendar.HOUR_OF_DAY) >= 21
    val targetWeekday: Int
    val targetWeek: Int
    val targetCalendar: Calendar

    if (isShowingTomorrow) {
        targetCalendar = Calendar.getInstance().apply { add(Calendar.DAY_OF_MONTH, 1) }
        targetWeekday = targetCalendar.let { ... }
        targetWeek = if (targetWeekday == 1) currentWeek + 1 else currentWeek
    } else {
        targetCalendar = Calendar.getInstance()
        targetWeekday = Calendar.getInstance().let { ... }
        targetWeek = currentWeek
    }
    // ...
    OverviewUiState(
        // ...
        todayCourses = displayCourses,
        upcomingExams = exams.take(3),
        examHighlight = ExamHighlightSelector.select(exams),
        // ...
    )
}
```

关键点：21 点后切换为明日课表，若明天是周一则教学周 +1。

### 8.4 OverviewUiState 状态定义

[overview/ui/model/OverviewUiState.kt:7-34](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/model/OverviewUiState.kt#L7-L34)：

```kotlin
@Immutable
data class OverviewUiState(
    val isRefreshing: Boolean = false,
    val isSilentSyncing: Boolean = false,
    val isBoundStudent: Boolean = false,
    val isOnline: Boolean = false,
    val isElectricityBound: Boolean = false,
    val accountName: String = "掌上长理",
    val avatarUrl: String = "",
    val studentId: String = "",
    val dateText: String = "",
    val currentTerm: String = "",
    val currentWeek: Int = 1,
    val dataSourceLabel: String = "正在整理本地数据",
    val metrics: List<OverviewMetricUiModel> = emptyList(),
    val todayCourses: List<OverviewCourseUiModel> = emptyList(),
    val todayCourseMessage: String = "",
    val isShowingTomorrow: Boolean = false,
    val pendingHomeworks: List<OverviewHomeworkUiModel> = emptyList(),
    val pendingHomeworkMessage: String = "",
    val pendingTests: List<OverviewTestUiModel> = emptyList(),
    val pendingTestMessage: String = "",
    val upcomingExams: List<OverviewExamUiModel> = emptyList(),
    val examMessage: String = "",
    val examHighlight: ExamHighlightUiModel? = null,
    val refreshingKeys: Set<String> = emptySet(),
    val lastUpdatedText: Map<String, String> = emptyMap()
)
```

### 8.5 OverviewViewModel 刷新策略

[overview/viewmodel/OverviewViewModel.kt:26-49](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt#L26-L49)：

```kotlin
class OverviewViewModel : ViewModel() {
    companion object {
        private const val REFRESH_INTERVAL_MS = 30 * 60 * 1000L // 30 分钟
    }

    private val repository by lazy { OverviewRepository(PlanetApplication.appContext) }
    private val announcementRepository by lazy { AnnouncementRepository() }
    private var lastRefreshTimeMs: Long = 0L

    private val _uiState = MutableStateFlow(OverviewUiState())
    val uiState: StateFlow<OverviewUiState> = _uiState.asStateFlow()
    private val _announcementState = MutableStateFlow(AnnouncementState())
    val announcementState: StateFlow<AnnouncementState> = _announcementState.asStateFlow()

    init {
        _announcementState.value = announcementRepository.loadCached()
        load(preserveVisibleContent = false)
        refreshAnnouncements()
    }
}
```

[overview/viewmodel/OverviewViewModel.kt:139-172](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt#L139-L172)：

```kotlin
private fun load(preserveVisibleContent: Boolean) {
    loadJob?.cancel()
    loadJob = viewModelScope.launch {
        val currentState = _uiState.value
        val localState = repository.loadLocalState()
        val displayState = mergeForDisplay(
            current = currentState,
            incoming = localState,
            preserveVisibleContent = preserveVisibleContent
        ).copy(
            isSilentSyncing = localState.isBoundStudent && localState.isOnline
        )
        _uiState.value = displayState

        loadMoocFromCache()

        val shouldRefresh = localState.isBoundStudent && localState.isOnline &&
            (lastRefreshTimeMs == 0L || System.currentTimeMillis() - lastRefreshTimeMs >= REFRESH_INTERVAL_MS)

        if (shouldRefresh) {
            val refreshedState = repository.refreshState()
            lastRefreshTimeMs = System.currentTimeMillis()
            _uiState.value = mergeForDisplay(
                current = _uiState.value,
                incoming = refreshedState,
                preserveVisibleContent = true
            )
            // 异步刷新慕课网络数据
            refreshMoocData()
        } else if (localState.isBoundStudent && localState.isOnline) {
            Log.d(TAG, "Skip refresh, cache valid (lastRefresh=${lastRefreshTimeMs})")
        }
    }
}
```

刷新策略：
1. 先 `loadLocalState()` 立即上屏，避免白屏
2. 距上次成功刷新超过 30 分钟才走网络静默刷新
3. 网络数据回来后 `mergeForDisplay`，保留当前已有内容不被空数据覆盖

### 8.6 mergeForDisplay 防空覆盖

[overview/viewmodel/OverviewViewModel.kt:220-246](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt#L220-L246)：

```kotlin
private fun mergeForDisplay(
    current: OverviewUiState,
    incoming: OverviewUiState,
    preserveVisibleContent: Boolean
): OverviewUiState {
    if (!preserveVisibleContent) return incoming

    val keepCourses = current.todayCourses.isNotEmpty() && incoming.todayCourses.isEmpty()
    val keepExams = current.upcomingExams.isNotEmpty() && incoming.upcomingExams.isEmpty()
    val keepExamHighlight = current.examHighlight != null && incoming.examHighlight == null

    return incoming.copy(
        metrics = mergeMetrics(current.metrics, incoming.metrics),
        todayCourses = if (keepCourses) current.todayCourses else incoming.todayCourses,
        // ... 保留慕课数据不被 incoming 的空值覆盖 ...
        refreshingKeys = current.refreshingKeys,
        lastUpdatedText = current.lastUpdatedText
    )
}
```

### 8.7 OverviewScreen UI 结构

[overview/ui/compose/OverviewScreen.kt:148-190](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L148-L190)：

```kotlin
@Composable
fun OverviewScreen(
    viewModel: OverviewViewModel,
    onBindClick: () -> Unit,
    onQuickActionClick: (String) -> Unit
) {
    val state by viewModel.uiState.collectAsState()
    val announcementState by viewModel.announcementState.collectAsState()
    val lifecycleOwner = LocalLifecycleOwner.current

    LaunchedEffect(viewModel) {
        viewModel.onHomeVisible()
        while (true) {
            delay(AnnouncementRepository.FOREGROUND_REFRESH_INTERVAL_MS)
            viewModel.refreshAnnouncements()
        }
    }

    DisposableEffect(lifecycleOwner, viewModel) {
        val observer = LifecycleEventObserver { _, event ->
            if (event == Lifecycle.Event.ON_RESUME) {
                viewModel.onResume()
            }
        }
        lifecycleOwner.lifecycle.addObserver(observer)
        onDispose { lifecycleOwner.lifecycle.removeObserver(observer) }
    }
    // ...
}
```

卡片默认顺序见 [OverviewScreen.kt:116-146](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L116-L146)：

```kotlin
private val DefaultOverviewCardOrder = listOf("course", "score", "electric", "homework", "exam")
private val OverviewCardNames = mapOf(
    "course" to "我的课表",
    "score" to "成绩查询",
    "electric" to "宿舍电量",
    "homework" to "待提交作业",
    "exam" to "考试安排"
)
```

### 8.8 LegacyPortalDashboard 布局

[OverviewScreen.kt:607-672](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L607-L672)：

```kotlin
@Composable
private fun LegacyPortalDashboard(
    state: OverviewUiState,
    onMetricClick: (String) -> Unit,
    onQuickActionClick: (String) -> Unit,
    onRefreshMetric: (String) -> Unit,
    onRefreshSection: (String) -> Unit
) {
    val score = state.metrics.firstOrNull { it.id.contains("score", true) }
    val electric = state.metrics.firstOrNull { it.id.contains("Electronic", true) }
    Column(modifier = Modifier.fillMaxWidth(), verticalArrangement = Arrangement.spacedBy(10.dp)) {
        HomeCourseHero(state) { onQuickActionClick("schedule") }
        Row(...) {
            HomeMetricCard(title = "成绩查询", metric = score, accent = Color(0xFF16B764), ...)
            HomeMetricCard(title = "宿舍电量", metric = electric, accent = Color(0xFF159CE4), ...)
        }
        Row(...) {
            ExamHighlightCard(highlight = state.examHighlight, ..., onClick = { onQuickActionClick("exam") })
            HomeStatusCard(title = "待提交作业", ..., onClick = { onQuickActionClick("homework") })
        }
    }
}
```

### 8.9 HomeMiniTrend 折线图

[OverviewScreen.kt:842-880](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L842-L880)：

```kotlin
@Composable
private fun HomeMiniTrend(values: List<Float>, accent: Color) {
    Canvas(modifier = Modifier.fillMaxWidth().height(44.dp)) {
        if (values.size < 2) {
            drawLine(color = accent.copy(alpha = 0.18f),
                start = Offset(0f, size.height * 0.62f),
                end = Offset(size.width, size.height * 0.62f),
                strokeWidth = 3f)
            return@Canvas
        }
        // 归一化 + 面积渐变 + 折线 + 圆点
    }
}
```

---

## 9. 考试高亮选择器 ExamHighlightSelector

### 9.1 完整实现

[overview/data/ExamHighlightSelector.kt:8-46](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/ExamHighlightSelector.kt#L8-L46)：

```kotlin
internal object ExamHighlightSelector {
    fun select(
        exams: List<OverviewExamUiModel>,
        now: LocalDateTime = LocalDateTime.now()
    ): ExamHighlightUiModel {
        val today = now.toLocalDate()
        val tomorrow = today.plusDays(1)
        val todayExams = exams.filter { it.startTime?.toLocalDate() == today }.sortedBy { it.startTime }
        val tomorrowExams = exams.filter { it.startTime?.toLocalDate() == tomorrow }.sortedBy { it.startTime }

        val current = todayExams.firstOrNull { exam ->
            val start = exam.startTime
            val end = exam.endTime
            start != null && end != null && !now.isBefore(start) && now.isBefore(end)
        }
        val upcoming = todayExams.firstOrNull { exam ->
            exam.startTime?.let(now::isBefore) == true
        }

        return when {
            current != null -> current.toHighlight(ExamHighlightType.TODAY_CURRENT, "正在考试")
            upcoming != null -> upcoming.toHighlight(ExamHighlightType.TODAY_UPCOMING, "今日考试")
            tomorrowExams.isNotEmpty() -> tomorrowExams.first().toHighlight(
                type = ExamHighlightType.TOMORROW,
                title = "明日考试",
                moreCount = tomorrowExams.size - 1
            )
            else -> ExamHighlightUiModel(
                type = ExamHighlightType.EMPTY,
                title = "考试安排",
                courseName = "今天没有考试哦",
                timeText = "继续保持！"
            )
        }
    }
}
```

### 9.2 优先级总结

```
1. 今天正在进行的考试 → 显示「正在考试」
        ↓
2. 今天下一场考试 → 显示「今日考试」
        ↓
3. 明天第一场考试 → 显示「明日考试」+ moreCount
        ↓
4. 空状态 → 「今天没有考试哦 / 继续保持！」
```

---

## 10. 电费查询模块

### 10.1 ElectricityRepository

[feature/common/data/repository/ElectricityRepository.kt:7-30](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt#L7-L30)：

```kotlin
class ElectricityRepository {
    private val mmkv by lazy { MMKV.defaultMMKV() }

    fun getBinding(): ElectricityBinding? {
        val school = mmkv.decodeString(KEY_SCHOOL, DEFAULT_SCHOOL).orEmpty()
        val dorm = mmkv.decodeString(KEY_DORM, DEFAULT_DORM).orEmpty()
        val room = mmkv.decodeString(KEY_ROOM, "").orEmpty()
        if (school == DEFAULT_SCHOOL || dorm == DEFAULT_DORM || room.isBlank()) return null
        return ElectricityBinding(school = school, dorm = dorm, room = room)
    }

    fun shouldAutoRefresh(): Boolean {
        val binding = getBinding() ?: return false
        val snapshot = OverviewLocalCache.getElectricitySnapshot() ?: return true
        if (snapshot.lastTime <= 0L) return true
        return System.currentTimeMillis() - snapshot.lastTime >= AUTO_REFRESH_INTERVAL_MS
    }
}
```

### 10.2 query 网络查询

[feature/common/data/repository/ElectricityRepository.kt:32-56](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt#L32-L56)：

```kotlin
suspend fun query(force: Boolean): ElectricityQueryResult? {
    val binding = getBinding() ?: return null
    if (!force) {
        val snapshot = OverviewLocalCache.getElectricitySnapshot()
        if (snapshot != null && System.currentTimeMillis() - snapshot.lastTime < AUTO_REFRESH_INTERVAL_MS) {
            return getCachedResult()
        }
    }

    val response = CampusCardHelper.queryElectricity(
        binding.school,
        binding.dorm,
        processDormAndRoom(binding.dorm, binding.room)
    ) ?: return ElectricityQueryResult(rawValue = "无数据", numericValue = null, fromCache = false)

    val value = extractElectricityValue(response.toString())
    if (value != null) {
        OverviewLocalCache.saveElectricitySnapshot(value)
    }
    return ElectricityQueryResult(rawValue = response.toString(), numericValue = value, fromCache = false)
}
```

### 10.3 saveBinding 切换寝室清空历史

[feature/common/data/repository/ElectricityRepository.kt:58-66](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt#L58-L66)：

```kotlin
fun saveBinding(school: String, dorm: String, room: String) {
    val oldBinding = getBinding()
    if (oldBinding != null && oldBinding != ElectricityBinding(school, dorm, room)) {
        OverviewLocalCache.clearElectricityHistory()
    }
    mmkv.encode(KEY_SCHOOL, school)
    mmkv.encode(KEY_DORM, dorm)
    mmkv.encode(KEY_ROOM, room)
}
```

### 10.4 processDormAndRoom A/B 栋前缀

[feature/common/data/repository/ElectricityRepository.kt:82-92](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt#L82-L92)：

```kotlin
private fun processDormAndRoom(dorm: String, room: String): String {
    val containsA = dorm.contains('A')
    val containsB = dorm.contains('B')
    val roomContainsLetter = room.any { it.isLetter() }

    return when {
        containsA && !roomContainsLetter -> "A$room"
        containsB && !roomContainsLetter -> "B$room"
        else -> room
    }
}
```

如果宿舍楼名包含 A/B 且房间号无字母前缀，自动补 A/B 前缀。

### 10.5 OverviewLocalCache 电量快照

[overview/data/local/OverviewLocalCache.kt:8-46](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/local/OverviewLocalCache.kt#L8-L46)：

```kotlin
object OverviewLocalCache {
    private const val CACHE_ID = "overview_local_cache"
    private const val KEY_ELECTRICITY_HISTORY = "electricity_history"
    private const val KEY_LAST_ELECTRICITY = "last_electricity"
    private const val KEY_PREV_ELECTRICITY = "prev_electricity"
    private const val MAX_ELECTRICITY_HISTORY_SIZE = 1000
    private const val HISTORY_RETENTION_DAYS = 100
    private val mmkv by lazy { MMKV.mmkvWithID(CACHE_ID) }

    fun saveElectricitySnapshot(value: Float) {
        appendElectricityHistory(value)
        val oldValue = mmkv.decodeFloat(KEY_LAST_ELECTRICITY, Float.NaN)
        val oldTime = mmkv.decodeLong(KEY_LAST_ELECTRICITY_TIME, 0L)
        if (!oldValue.isNaN() && oldTime > 0L) {
            mmkv.encode(KEY_PREV_ELECTRICITY, oldValue)
            mmkv.encode(KEY_PREV_ELECTRICITY_TIME, oldTime)
        }
        mmkv.encode(KEY_LAST_ELECTRICITY, value)
        mmkv.encode(KEY_LAST_ELECTRICITY_TIME, System.currentTimeMillis())
    }
}
```

### 10.6 getDailyUsage 按日累计

[overview/data/local/OverviewLocalCache.kt:80-124](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/local/OverviewLocalCache.kt#L80-L124)：

```kotlin
/**
 * Calculates daily consumption from consecutive real remaining-balance readings.
 * Positive balance jumps are recharges and are intentionally excluded.
 */
fun getDailyUsage(days: Int, now: Long = System.currentTimeMillis()): List<ElectricityUsageEntry> {
    if (days <= 0) return emptyList()
    // 按 0:00 切分时间桶
    val starts = LongArray(days + 1)
    // ...
    val usage = FloatArray(days)
    val covered = LongArray(days)
    getElectricityHistory()
        .sortedBy { it.timestamp }
        .zipWithNext()
        .forEach { (previous, current) ->
            val elapsed = current.timestamp - previous.timestamp
            val consumed = previous.value - current.value
            if (elapsed <= 0L || consumed < 0f) return@forEach  // 排除充值

            for (index in 0 until days) {
                val overlapStart = maxOf(previous.timestamp, starts[index])
                val overlapEnd = minOf(current.timestamp, starts[index + 1], now)
                val overlap = overlapEnd - overlapStart
                if (overlap > 0L) {
                    usage[index] += consumed * overlap.toFloat() / elapsed.toFloat()
                    covered[index] += overlap
                }
            }
        }
    // ...
}
```

按时间重叠比例分摊到对应日期桶，充值（consumed < 0）被排除。

### 10.7 estimateElectricityDays 剩余天数估算

[overview/data/OverviewRepository.kt:363-406](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L363-L406)：

```kotlin
private fun estimateElectricityDays(
    history: List<ElectricityHistoryEntry>,
    currentValue: Double
): Int? {
    if (history.size < 3 || currentValue <= 0.0) return null
    val now = System.currentTimeMillis()
    val recentHistory = history.sortedBy { it.timestamp }
        .filter { now - it.timestamp <= TimeUnit.DAYS.toMillis(21) }
        .takeLast(12)
    if (recentHistory.size < 3) return null

    val dailyRates = recentHistory.zipWithNext()
        .mapNotNull { (previous, current) ->
            val deltaHours = (current.timestamp - previous.timestamp).toDouble() / TimeUnit.HOURS.toMillis(1)
            val deltaValue = previous.value - current.value
            when {
                deltaHours !in 1.0..96.0 -> null
                deltaValue <= 0.05f -> null
                else -> UsageRate(
                    dailyUsage = deltaValue / deltaHours * 24.0,
                    weight = (deltaHours.coerceAtMost(24.0) / 24.0).coerceAtLeast(0.2)
                )
            }
        }
    if (dailyRates.size < 2) return null

    // 中位数过滤 + 加权平均
    val medianRate = dailyRates.map { it.dailyUsage }.sorted().let { sorted -> ... }
    val filteredRates = dailyRates.filter { abs(it.dailyUsage - medianRate) <= maxOf(0.5, medianRate * 0.4) }
    if (filteredRates.isEmpty()) return null

    val weightedUsage = filteredRates.sumOf { it.dailyUsage * it.weight } / filteredRates.sumOf { it.weight }
    if (weightedUsage !in 0.5..20.0) return null

    return kotlin.math.ceil(currentValue / weightedUsage).toInt().coerceAtLeast(1)
}
```

算法：
1. 取最近 21 天内最多 12 条历史
2. 计算相邻记录每小时用电量，过滤异常值（<= 0 或 > 5 kWh/h）
3. 取中位数，过滤偏离中位数 > 40% 的样本
4. 用加权平均（权重 = 采样时长占 24 小时的比例）得到日用电量
5. `剩余电量 / 日用电量 = 剩余天数`

### 10.8 ElectronicScreen 前台自动采样

[feature/common/compose_ui/ElectronicScreen.kt:176-182](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/compose_ui/ElectronicScreen.kt#L176-L182)：

```kotlin
// Foreground automatic sampling. The first short delay lets Init restore the
// saved binding; later samples arrive without requiring the query button.
LaunchedEffect(Unit) {
    delay(1_500L)
    while (true) {
        viewModel.processIntent(ElectronicContract.Intent.AutoRefresh)
        delay(5 * 60 * 1_000L)
    }
}
```

---

## 11. 成绩查询与 GPA 计算

### 11.1 preprocessGrades 重修/补考去重

[overview/data/OverviewRepository.kt:334-346](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L334-L346)：

```kotlin
private fun preprocessGrades(rawData: List<Grade>): List<Grade> {
    return rawData
        .groupBy { it.name }
        .map { (_, grades) ->
            val retakeGrade = grades.find { grade ->
                grade.upperReItem.isNotBlank() ||
                    grade.property.contains("重修", ignoreCase = true) ||
                    grade.property.contains("补考", ignoreCase = true)
            }
            retakeGrade ?: grades.maxByOrNull { it.grade.toDoubleOrNull() ?: 0.0 }!!
        }
}
```

规则：
- 同一门课有重修/补考记录 → 取重修/补考成绩
- 否则取最高分

### 11.2 GPA 加权计算

[overview/data/OverviewRepository.kt:266-326](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L266-L326)：

```kotlin
private fun buildMetrics(
    grades: List<Grade>,
    electricitySnapshot: ElectricitySnapshot?,
    isElectricityBound: Boolean
): List<OverviewMetricUiModel> {
    val processedGrades = preprocessGrades(grades)
    val totalCredits = processedGrades.sumOf { it.score.toDoubleOrNull() ?: 0.0 }
    val gpa = if (totalCredits > 0) {
        processedGrades.sumOf { (it.score.toDoubleOrNull() ?: 0.0) * (it.point.toDoubleOrNull() ?: 0.0) } / totalCredits
    } else { 0.0 }
    val averageScore = if (totalCredits > 0) {
        processedGrades.sumOf { (it.grade.toDoubleOrNull() ?: 0.0) * (it.score.toDoubleOrNull() ?: 0.0) } / totalCredits
    } else { 0.0 }
    // ... 构造成绩 / 电量 OverviewMetricUiModel ...
}
```

公式：
- `GPA = Σ(学分 × 绩点) / Σ学分`
- `平均分 = Σ(学分 × 成绩) / Σ学分`

### 11.3 ScoreCache 容错

[feature/common/data/local/mmkv/ScoreCache.kt:30-52](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ScoreCache.kt#L30-L52)：

```kotlin
fun getGrades(): List<Grade>? {
    val json = mmkv.decodeString("grades") ?: return null
    return try {
        val grades: List<Grade>? = gson.fromJson(json, object : TypeToken<List<Grade>>() {}.type)
        if (grades == null) { mmkv.removeValueForKey("grades"); return null }
        val normalized = grades.mapNotNull(::normalizeGrade)
        if (normalized.isEmpty()) { mmkv.removeValueForKey("grades"); return null }
        if (normalized.size != grades.size) { mmkv.encode("grades", gson.toJson(normalized)) }
        normalized
    } catch (e: Exception) { mmkv.removeValueForKey("grades"); null }
}
```

反序列化失败自动清缓存，字段缺失自动归一化。

### 11.4 ExamArrangementCache 完整性校验

[feature/common/data/local/mmkv/ExamArrangementCache.kt:38-40](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ExamArrangementCache.kt#L38-L40)：

```kotlin
private fun ExamArrange.isCacheCompatible(): Boolean = runCatching {
    courseNameval.isNotBlank() && examTime.isNotBlank() && campus.isNotBlank() && examRoomval.isNotBlank()
}.getOrDefault(false)
```

任一字段为空 → 整个缓存作废。

---

## 12. 课表模块

### 12.1 fetchCourses 网络拉取

[overview/data/OverviewRepository.kt:216-253](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L216-L253)：

```kotlin
private suspend fun fetchCourses(term: String): List<TimeTableMySubject>? {
    return when (val result = runCatching { EducationHelper.getCourseScheduleByTerm("", term) }.getOrNull()) {
        is Resource.Success -> {
            val subjects = result.data.courses.map {
                val weekInfo = parseWeeks(it.weeks)
                TimeTableMySubject(
                    courseName = it.courseName,
                    classroom = it.classroom,
                    teacher = it.teacher,
                    weeks = weekInfo.weeks,
                    start = weekInfo.start,
                    step = weekInfo.step,
                    weekday = it.weekday.toInt(),
                    term = term,
                    studentId = StudentInfoManager.studentId,
                    studentPassword = StudentInfoManager.studentPassword
                )
            }.distinctBy {
                "${it.courseName}${it.classroom}${it.teacher}${it.start}${it.step}${it.weekday}${it.term}"
            }
            // ...deleteNetworkCoursesByTerm + insertCourses...
        }
        else -> null
    }
}
```

### 12.2 parseWeeks 周次解析正则

[overview/data/OverviewRepository.kt:445-470](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt#L445-L470)：

```kotlin
private fun parseWeeks(weekJson: String): WeekJsonInfo {
    val pattern = Pattern.compile(
        "(\\d+(?:-\\d+)?(?:,\\d+(?:-\\d+)?)*)\\((周|单周|双周)?\\)?\\[(\\d{2})(?:-(\\d{2}))?(?:-(\\d{2}))?(?:-(\\d{2}))?节\\]"
    )
    // 解析如 "1-16(周)[01-02节]" 的字符串
}
```

解析 `"1-16(周)[03-04节]"` → 周次列表 `[1..16]`、开始节 `3`、持续节数 `2`。

---

## 13. 慕课 MOOC 模块

### 13.1 fetchMoocCourses 核心拉取

[feature/mooc/data/MoocDataHelper.kt:96-211](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt#L96-L211)：

```kotlin
suspend fun fetchMoocCourses(
    username: String,
    password: String,
    forceRefresh: Boolean = false
): List<CourseItem> = withContext(Dispatchers.IO) {
    if (forceRefresh) {
        clearMoocSession()
        val loginResult = awaitLoginResult(username, password)
        if (loginResult !is Resource.Success || !loginResult.data) {
            val errorMsg = (loginResult as? Resource.Error)?.msg ?: "慕课登录失败"
            throw IllegalStateException(errorMsg)
        }
    }

    val courseResult = executeWithRetry(username, password) {
        repository.getCourseNamesWithPendingHomeworks().filter { it !is Resource.Loading }.first()
    }

    when (courseResult) {
        is Resource.Success -> {
            val homeworkCourses = courseResult.data.map {
                PendingAssignmentCourse(id = it.id.cleanCourseId(), name = it.name)
            }.distinctBy { it.id }

            val homeworkCourseData = coroutineScope {
                homeworkCourses.map { course ->
                    async {
                        val homeworks = runCatching { executeWithRetry(...) { repository.getCourseHomeworks(course.id)... } }.getOrNull()
                        val tests = runCatching { executeWithRetry(...) { repository.getCourseTests(course.id)... } }.getOrNull()
                        CourseLoadResult(course = course, homeworks = homeworks, tests = tests)
                    }
                }.awaitAll()
            }

            val allCoursesResult = executeWithRetry(...) { repository.getCourses()... }
            // 合并待作业课程与全部课程元数据
            MoocLocalCache.saveCourseItems(courseItems)
            MoocLocalCache.markSuccessfulRefresh()
            courseItems
        }
        // ...
    }
}
```

### 13.2 executeWithRetry 重试 + 重登

[feature/mooc/data/MoocDataHelper.kt:48-69](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt#L48-L69)：

```kotlin
suspend fun <T> executeWithRetry(
    username: String,
    password: String,
    block: suspend () -> Resource<T>
): Resource<T> {
    val first = block()
    if (first !is Resource.Error || !shouldRetryNetworkError(first.msg)) {
        return first
    }
    withContext(Dispatchers.IO) { clearMoocSession() }
    val relogin = awaitLoginResult(username, password)
    if (relogin is Resource.Success && relogin.data) {
        return block()
    }
    return if (relogin is Resource.Error) Resource.Error(relogin.msg) else first
}
```

### 13.3 extractPendingHomeworks 提取待提交作业

[feature/mooc/data/MoocDataHelper.kt:243-263](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt#L243-L263)：

```kotlin
fun extractPendingHomeworks(courseItems: List<CourseItem>): List<OverviewHomeworkUiModel> {
    return courseItems.flatMap { courseItem ->
        courseItem.homeworks
            .filter { it.homework.canSubmit && !it.homework.submitStatus }
            .map { hwItem ->
                val deadlineDate = parseDateOrNull(hwItem.homework.deadline)
                val remainingMillis = deadlineDate?.time?.minus(System.currentTimeMillis())
                val isUrgent = remainingMillis != null && remainingMillis in 1 until TimeUnit.DAYS.toMillis(1)
                OverviewHomeworkUiModel(
                    id = "${courseItem.course.id}_${hwItem.homework.id}",
                    title = hwItem.homework.title,
                    courseName = courseItem.course.name,
                    deadlineText = hwItem.homework.deadline,
                    urgencyText = buildUrgencyText(remainingMillis),
                    isUrgent = isUrgent
                )
            }
    }.sortedBy { item -> parseDateOrNull(item.deadlineText)?.time ?: Long.MAX_VALUE }
}
```

### 13.4 buildUrgencyText 紧迫度文案

[feature/mooc/data/MoocDataHelper.kt:293-306](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt#L293-L306)：

```kotlin
fun buildUrgencyText(remainingMillis: Long?): String {
    if (remainingMillis == null) return ""
    if (remainingMillis <= 0L) return "已截止"
    if (remainingMillis < TimeUnit.HOURS.toMillis(1)) {
        val minutes = (remainingMillis / TimeUnit.MINUTES.toMillis(1)).coerceAtLeast(1)
        return "${minutes}分钟内截止"
    }
    if (remainingMillis < TimeUnit.DAYS.toMillis(1)) {
        val hours = (remainingMillis / TimeUnit.HOURS.toMillis(1)).coerceAtLeast(1)
        return "${hours}小时内截止"
    }
    val days = (remainingMillis / TimeUnit.DAYS.toMillis(1)).coerceAtLeast(1)
    return "${days}天内截止"
}
```

### 13.5 MoocLocalCache

[feature/mooc/data/local/MoocLocalCache.kt:8-40](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/local/MoocLocalCache.kt#L8-L40)：

```kotlin
object MoocLocalCache {
    private const val CACHE_ID = "mooc_local_cache"
    private const val KEY_COURSE_ITEMS = "course_items"
    private const val KEY_LAST_SUCCESSFUL_REFRESH_TIME = "last_successful_refresh_time"

    fun saveCourseItems(items: List<CourseItem>) {
        mmkv.encode(KEY_COURSE_ITEMS, gson.toJson(items))
    }

    fun markSuccessfulRefresh() {
        mmkv.encode(KEY_LAST_SUCCESSFUL_REFRESH_TIME, System.currentTimeMillis())
    }
}
```

---

## 14. 校历与学期周次计算

### 14.1 getCurrentTerm 月份兜底

[common/cache/CommonInfo.kt:23-103](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/cache/CommonInfo.kt#L23-L103)：

```kotlin
object CommonInfo {
    const val MAX_TEACHING_WEEK: Int = 20
    private const val SHANGHAI_TZ_ID = "Asia/Shanghai"
    private val shanghaiTz: TimeZone get() = TimeZone.getTimeZone(SHANGHAI_TZ_ID)

    val termMap: Map<String, String> = mapOf(
        "2025-2026-2" to "2026-03-09 00:00:00",
        "2025-2026-1" to "2025-09-08 00:00:00",
        "2024-2025-2" to "2025-02-24 00:00:00",
        // ... 2019-2020-1 等
    )

    /**
     * 计算当前学期代码（统一规则，全 App 共用）。
     * 月份兜底规则（基于上海时区）：
     *  - 7-12 月 → `当年-下一年-1`（秋季）
     *  - 2-6 月 → `上一年-当年-2`（春季）
     *  - 1 月   → `上一年-当年-1`（上学年延续）
     */
    fun getCurrentTerm(): String {
        resolveCurrentTermByDate()?.let { return it }
        val calendar = Calendar.getInstance(shanghaiTz)
        val year = calendar.get(Calendar.YEAR)
        val month = calendar.get(Calendar.MONTH) + 1
        return when {
            month >= 7 -> "$year-${year + 1}-1"
            month >= 2 -> "${year - 1}-${year}-2"
            else -> "${year - 1}-${year}-1"
        }
    }
}
```

### 14.2 getTermStartDate 统一获取开学日期

[common/cache/CommonInfo.kt:156-160](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/cache/CommonInfo.kt#L156-L160)：

```kotlin
fun getTermStartDate(term: String): String? {
    if (term.isBlank()) return null
    SemesterCalendarRepository.getTermStartDate(term)?.let { return it }
    return termMap[term]
}
```

### 14.3 computeWeekNumber 第几周计算

[common/cache/CommonInfo.kt:270-282](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/cache/CommonInfo.kt#L270-L282)：

```kotlin
private fun computeWeekNumber(startTimeStr: String): Int? {
    val startDate = parseTermDate(startTimeStr) ?: return null
    val today = Calendar.getInstance().apply {
        set(Calendar.HOUR_OF_DAY, 0)
        set(Calendar.MINUTE, 0)
        set(Calendar.SECOND, 0)
        set(Calendar.MILLISECOND, 0)
    }.time
    val diffMs = today.time - startDate.time
    if (diffMs < 0) return 0  // 未开学
    val days = diffMs / (1000L * 60 * 60 * 24)
    return (days / 7 + 1).toInt()
}
```

---

## 15. 公告系统

### 15.1 数据模型

[overview/announcement/AnnouncementModels.kt:5-45](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementModels.kt#L5-L45)：

```kotlin
enum class AnnouncementLevel {
    NORMAL, IMPORTANT, URGENT;
    companion object {
        fun fromWire(value: String?): AnnouncementLevel = when (value?.lowercase()) {
            "urgent" -> URGENT
            "important" -> IMPORTANT
            else -> NORMAL
        }
    }
}

@Immutable
data class AnnouncementUiModel(
    val id: String,
    val revision: Int,
    val level: AnnouncementLevel,
    val title: String,
    val summary: String,
    val content: List<String>,
    val publishAt: String,
    val displayTime: String,
    val expireAt: String,
    val actionText: String,
    val actionUrl: String,
    val isRead: Boolean
) {
    val readKey: String get() = "$id@$revision"
}

@Immutable
data class AnnouncementState(
    val announcements: List<AnnouncementUiModel> = emptyList(),
    val unreadCount: Int = 0,
    val isRefreshing: Boolean = false,
    val hasLoaded: Boolean = false,
    val lastUpdatedAtMillis: Long = 0L,
    val urgentToPresent: AnnouncementUiModel? = null
)
```

### 15.2 AnnouncementRepository 常量与节流

[overview/announcement/AnnouncementRepository.kt:16-29](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementRepository.kt#L16-L29)：

```kotlin
companion object {
    private const val MANIFEST_URL =
        "https://gitee.com/liubo2355-hub/csustpocket/raw/master/config/announcements.json"
    private const val CACHE_ID = "announcement_cache"
    private const val KEY_MANIFEST = "manifest"
    private const val KEY_ETAG = "etag"
    private const val KEY_LAST_MODIFIED = "last_modified"
    private const val KEY_LAST_CHECK = "last_check"
    private const val KEY_READ = "read_keys"
    private const val KEY_URGENT_SHOWN = "urgent_shown_keys"
    const val HOME_REFRESH_THROTTLE_MS = 60_000L        // 首页 60 秒节流
    const val FOREGROUND_REFRESH_INTERVAL_MS = 5 * 60_000L  // 前台 5 分钟轮询
}
```

### 15.3 refresh ETag/304 缓存策略

[overview/announcement/AnnouncementRepository.kt:42-90](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementRepository.kt#L42-L90)：

```kotlin
suspend fun refresh(force: Boolean = false): AnnouncementState = withContext(Dispatchers.IO) {
    val nowMillis = System.currentTimeMillis()
    val lastCheck = mmkv.decodeLong(KEY_LAST_CHECK, 0L)
    if (!force && nowMillis - lastCheck < HOME_REFRESH_THROTTLE_MS) {
        return@withContext stateFromCachedManifest()
    }

    try {
        val requestBuilder = Request.Builder()
            .url(MANIFEST_URL)
            .header("User-Agent", "CSUSTPocket-Android/${BuildConfig.VERSION_NAME}")
            .header("Cache-Control", "no-cache")
        mmkv.decodeString(KEY_ETAG, "")?.takeIf(String::isNotBlank)?.let {
            requestBuilder.header("If-None-Match", it)
        }
        mmkv.decodeString(KEY_LAST_MODIFIED, "")?.takeIf(String::isNotBlank)?.let {
            requestBuilder.header("If-Modified-Since", it)
        }

        client.newCall(requestBuilder.build()).execute().use { response ->
            when {
                response.code == 304 -> Unit  // 直接用缓存
                !response.isSuccessful -> throw IOException("公告源 HTTP ${response.code}")
                else -> {
                    val raw = response.body?.string().orEmpty()
                    val parsed = gson.fromJson(raw, AnnouncementManifestDto::class.java)
                        ?: throw IOException("公告清单格式无效")
                    mmkv.encode(KEY_MANIFEST, raw)
                    response.header("ETag")?.let { mmkv.encode(KEY_ETAG, it) }
                    response.header("Last-Modified")?.let { mmkv.encode(KEY_LAST_MODIFIED, it) }
                }
            }
        }
    } catch (error: Throwable) {
        Log.w(TAG, "公告刷新失败，继续使用缓存", error)
    } finally {
        // A failed request is still a check. This prevents every quick return to
        // the home screen from starting another network request on poor networks.
        mmkv.encode(KEY_LAST_CHECK, nowMillis)
    }
    stateFromCachedManifest().copy(lastUpdatedAtMillis = mmkv.decodeLong(KEY_LAST_CHECK, 0L))
}
```

### 15.4 AnnouncementPolicy 过滤规则

[overview/announcement/AnnouncementPolicy.kt:7-32](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementPolicy.kt#L7-L32)：

```kotlin
internal object AnnouncementPolicy {
    fun visibleAnnouncements(
        manifest: AnnouncementManifestDto,
        versionCode: Int,
        now: OffsetDateTime,
        readKeys: Set<String>,
        shownUrgentKeys: Set<String>
    ): AnnouncementState {
        val items = manifest.announcements.orEmpty()
            .mapNotNull { dto -> dto.toUiModelOrNull(versionCode, now, readKeys) }
            .sortedWith(
                compareByDescending<AnnouncementUiModel> { it.level.priority }
                    .thenByDescending { parseTime(it.publishAt)?.toInstant() }
            )
        val urgent = items.firstOrNull {
            it.level == AnnouncementLevel.URGENT && !it.isRead && it.readKey !in shownUrgentKeys
        }
        return AnnouncementState(
            announcements = items,
            unreadCount = items.count { !it.isRead },
            hasLoaded = true,
            urgentToPresent = urgent
        )
    }
}
```

[AnnouncementPolicy.kt:34-65](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementPolicy.kt#L34-L65)：

```kotlin
private fun AnnouncementDto.toUiModelOrNull(...): AnnouncementUiModel? {
    // 版本范围过滤 + 启用过滤 + 时间窗口过滤（发布前/过期）
    if (versionCode < (minVersionCode ?: 1) || versionCode > (maxVersionCode ?: Int.MAX_VALUE)) return null
    val publishTime = parseTime(publishAt)
    val expireTime = parseTime(expireAt)
    if (publishTime != null && now.isBefore(publishTime)) return null
    if (expireTime != null && !now.isBefore(expireTime)) return null
    // ...
}

private val AnnouncementLevel.priority: Int
    get() = when (this) {
        AnnouncementLevel.URGENT -> 3
        AnnouncementLevel.IMPORTANT -> 2
        AnnouncementLevel.NORMAL -> 1
    }
```

### 15.5 AnnouncementCenterDialog

[overview/ui/compose/AnnouncementCenter.kt:57-98](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/AnnouncementCenter.kt#L57-L98)：

```kotlin
@Composable
internal fun AnnouncementCenterDialog(
    state: AnnouncementState,
    onDismiss: () -> Unit,
    onRefresh: () -> Unit,
    onMarkRead: (String) -> Unit
) {
    var selected by remember { mutableStateOf<AnnouncementUiModel?>(null) }
    Dialog(
        onDismissRequest = { if (selected != null) selected = null else onDismiss() },
        properties = DialogProperties(usePlatformDefaultWidth = false, decorFitsSystemWindows = false)
    ) {
        Box(Modifier.fillMaxSize().background(AppTheme.colors.overviewPageBackgroundColor)...) {
            if (selected == null) {
                AnnouncementList(state = state, onBack = onDismiss, onRefresh = onRefresh, onOpen = { announcement ->
                    onMarkRead(announcement.readKey)
                    selected = announcement.copy(isRead = true)
                })
            } else {
                AnnouncementDetail(announcement = selected!!, onBack = { selected = null })
            }
        }
    }
}
```

### 15.6 UrgentAnnouncementDialog

[AnnouncementCenter.kt:347-402](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/AnnouncementCenter.kt#L347-L402)：

```kotlin
@Composable
internal fun UrgentAnnouncementDialog(
    announcement: AnnouncementUiModel,
    onDismiss: () -> Unit,
    onOpenCenter: () -> Unit
) {
    Dialog(onDismissRequest = onDismiss) {
        HyperSurface(...) {
            Column(Modifier.padding(22.dp), horizontalAlignment = Alignment.CenterHorizontally) {
                Row(horizontalArrangement = Arrangement.spacedBy(12.dp)) {
                    HyperButton("稍后查看", onDismiss, modifier = Modifier.weight(1f), primary = false)
                    HyperButton("查看详情", onOpenCenter, modifier = Modifier.weight(1f))
                }
            }
        }
    }
}

private val AnnouncementLevel.accentColor: Color
    get() = when (this) {
        AnnouncementLevel.NORMAL -> Color(0xFF2589EF)
        AnnouncementLevel.IMPORTANT -> Color(0xFFF39A2E)
        AnnouncementLevel.URGENT -> Color(0xFFFF4D4F)
    }
```

---

## 16. 更新检查与下载安装

### 16.1 AppUpdateChecker 统一入口

[common/update/AppUpdateChecker.kt:13-32](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/update/AppUpdateChecker.kt#L13-L32)：

```kotlin
/**
 * 应用更新检查的统一入口，确保自动检查和手动检查使用相同的版本信息与并发策略。
 */
object AppUpdateChecker {
    private val store by lazy(LazyThreadSafetyMode.SYNCHRONIZED) { UserStore() }

    fun check(context: Context, manual: Boolean = false) {
        val packageInfo = try {
            context.packageManager.getPackageInfo(context.packageName, 0)
        } catch (_: PackageManager.NameNotFoundException) {
            if (manual) CustomToast.showMessage(context, "无法读取当前版本")
            return
        }

        store.dispatch(
            UserAction.QueryIsLastedApk(
                context = context,
                versionCode = PackageInfoCompat.getLongVersionCode(packageInfo),
                versionName = packageInfo.versionName.orEmpty(),
                manual = manual
            )
        )
    }
}
```

### 16.2 UserStore 更新源配置

[common/redux/store/UserStore.kt:41-59](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L41-L59)：

```kotlin
class UserStore : Store<UserState, UserAction>() {
    companion object {
        private const val GITEE_MANIFEST_URL =
            "https://gitee.com/liubo2355-hub/csustpocket/raw/master/update/latest.json"
        private const val GITHUB_API = "https://api.github.com/repos/liubo2355-hub/CSUSTPocket/releases/latest"
        private const val AUTO_CHECK_INTERVAL_MS = 30 * 60 * 1000L
        private const val MAX_UPDATE_ATTEMPTS = 3
        private val updateCheckInProgress = AtomicBoolean(false)
        private val bindingInProgress = AtomicBoolean(false)
        private val lastAutomaticCheckAt = AtomicLong(0L)
        private val updateDialogShowing = AtomicBoolean(false)
        @Volatile private var lastPromptedVersion: String? = null
    }
}
```

### 16.3 QueryIsLastedApk 冷却 + 并发锁

[common/redux/store/UserStore.kt:68-143](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L68-L143)：

```kotlin
is UserAction.QueryIsLastedApk -> {
    val now = System.currentTimeMillis()
    if (!action.manual && now - lastAutomaticCheckAt.get() < AUTO_CHECK_INTERVAL_MS) {
        Log.d(TAG, "跳过更新检查：仍在自动检查冷却期内")
        return
    }
    if (!updateCheckInProgress.compareAndSet(false, true)) {
        if (action.manual) CustomToast.showMessage(action.context, "正在检查更新，请稍候")
        return
    }
    if (action.manual) CustomToast.showMessage(action.context, "正在检查更新…")
    else lastAutomaticCheckAt.set(now)
    storeScope.launch {
        try {
            val update = fetchLatestUpdateWithFallback()
            if (update.versionName.isNotBlank()) {
                val hasUpdate = if (update.versionCode > 0) {
                    update.versionCode > action.versionCode
                } else {
                    isNewerVersion(update.versionName, action.versionName)
                }
                if (hasUpdate) {
                    handler.post {
                        showUpdateDialogIfPossible(
                            action = action,
                            latestVersion = update.versionName,
                            updateContent = update.releaseNotes.ifBlank { "新版 ${update.versionName}" },
                            downloadUrl = update.downloadUrl.ifBlank { "https://github.com/liubo2355-hub/CSUSTPocket/releases/latest" }
                        )
                    }
                } else if (action.manual) {
                    handler.post { CustomToast.showMessage(action.context, "已是最新版本 v${action.versionName}") }
                }
            }
        } catch (error: Throwable) { /* ... */ }
        finally { updateCheckInProgress.set(false) }
    }
    currentState
}
```

### 16.4 fetchLatestUpdateWithFallback Gitee → GitHub 降级

[common/redux/store/UserStore.kt:343-371](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L343-L371)：

```kotlin
private suspend fun fetchLatestUpdateWithFallback(): LatestUpdateInfo {
    try {
        val manifest = fetchDomesticManifestWithRetry()
        if (manifest.versionName.isBlank() || manifest.apkUrl.isBlank()) {
            throw IOException("Gitee 更新清单缺少版本号或 APK 地址")
        }
        automaticFailureNotified = false
        return LatestUpdateInfo(
            versionCode = manifest.versionCode,
            versionName = manifest.versionName.trimStart('v'),
            releaseNotes = manifest.releaseNotes,
            downloadUrl = manifest.apkUrl,
            source = "Gitee"
        )
    } catch (error: Throwable) {
        Log.w(TAG, "Gitee 国内更新源不可用，切换 GitHub 备用源", error)
    }

    val release = fetchGitHubLatestReleaseWithRetry()
    val apkAsset = release.assets.firstOrNull { it.name.endsWith(".apk", ignoreCase = true) }
    return LatestUpdateInfo(
        versionCode = 0,
        versionName = release.tagName.trimStart('v'),
        releaseNotes = release.body,
        downloadUrl = apkAsset?.downloadUrl.orEmpty(),
        source = "GitHub"
    )
}
```

### 16.5 isNewerVersion 版本号比较

[common/redux/store/UserStore.kt:439-485](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L439-L485)：

```kotlin
private fun isNewerVersion(latestVersion: String, currentVersion: String): Boolean {
    if (latestVersion == currentVersion) return false
    val appParts = parseVersion(currentVersion)
    val releaseParts = parseVersion(latestVersion)
    return if (appParts != null && releaseParts != null) {
        compareVersions(releaseParts, appParts) > 0
    } else {
        latestVersion > currentVersion
    }
}

/** 返回 >0 表示 a 比 b 新，==0 同版，<0 旧。 */
private fun compareVersions(a: List<Int>, b: List<Int>): Int {
    for (i in 0 until maxOf(a.size, b.size)) {
        val av = a.getOrElse(i) { 0 }; val bv = b.getOrElse(i) { 0 }
        if (av != bv) return av - bv
    }
    return 0
}
```

### 16.6 ReleaseNotesFormatter Markdown 清洗

[common/update/ReleaseNotesFormatter.kt:4-46](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/update/ReleaseNotesFormatter.kt#L4-L46)：

```kotlin
/** 将 GitHub Release 的 Markdown 文本转换成适合更新弹窗显示的纯文本条目。 */
object ReleaseNotesFormatter {
    private val bulletPattern = Regex("^[-*+]\\s+")
    private val orderedPattern = Regex("^\\d+[.)]\\s+")
    private val markdownLinkPattern = Regex("\\[([^]]+)]\\([^)]+\\)")

    fun format(raw: String): String {
        val normalized = raw
            .replace("\\r\\n", "\n").replace("\\n", "\n")
            .replace("\r\n", "\n").replace('\r', '\n')

        val result = mutableListOf<String>()
        normalized.lineSequence().forEach { sourceLine ->
            var line = sourceLine.trim()
            if (line.isBlank()) return@forEach
            if (line.contains("SHA-256", ignoreCase = true)) return@forEach
            line = line.trimStart('#').trim()
            val heading = line.trimEnd(':', '：').trim()
            if (heading.equals("本次更新", ignoreCase = true) ||
                heading.equals("更新内容", ignoreCase = true) ||
                heading.equals("更新说明", ignoreCase = true)) return@forEach

            line = markdownLinkPattern.replace(line, "$1").replace("**", "").replace("__", "").replace("`", "")
            line = when {
                bulletPattern.containsMatchIn(line) -> bulletPattern.replace(line, "• ")
                orderedPattern.containsMatchIn(line) -> orderedPattern.replace(line, "• ")
                else -> line
            }
            if (line.isNotBlank()) result += line
        }
        return result.joinToString("\n").take(1_200).ifBlank { "修复已知问题并提升使用体验" }
    }
}
```

### 16.7 DownloadService 前台服务 + 进度通知

[common/service/DownloadService.kt:27-83](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadService.kt#L27-L83)：

```kotlin
class DownloadService : Service() {
    companion object {
        const val NOTIFICATION_ID = 1001
        const val EXTRA_APK_URL = "extra_apk_url"
        const val APK_FILE_NAME = "update.apk"
        const val ACTION_DOWNLOAD = "action_Download"

        fun startDownload(context: Context, apkUrl: String) {
            val intent = Intent(context, DownloadService::class.java).apply {
                action = ACTION_DOWNLOAD
                putExtra(EXTRA_APK_URL, apkUrl)
            }
            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                context.startForegroundService(intent)
            } else {
                context.startService(intent)
            }
        }
    }

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        when (intent?.action) {
            ACTION_DOWNLOAD -> {
                val apkUrl = intent.getStringExtra(EXTRA_APK_URL).orEmpty()
                if (apkUrl.isBlank()) { stopSelfResult(startId); return START_NOT_STICKY }
                // 避免连续点击更新时并发写入同一个 APK 文件。
                if (downloadJob?.isActive == true) return START_NOT_STICKY
                deleteLocalApkIfExists()
                DownloadProgressStore.markStarted()
                showNotification(0, "开始更新", true)
                startDownload(apkUrl, startId)
            }
        }
        return START_NOT_STICKY
    }
}
```

[common/service/DownloadService.kt:129-170](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadService.kt#L129-L170)：

```kotlin
private suspend fun downloadApk(apkurl: String): File {
    val file = getLocalApkFile()
    val client = OkHttpClient.Builder().build()
    val request = Request.Builder().url(apkurl).build()
    client.newCall(request).execute().use { response ->
        check(response.isSuccessful) { "下载失败，HTTP ${response.code}" }
        val body = response.body
        val length = body.contentLength()
        body.byteStream().use { input ->
            FileOutputStream(file).use { output ->
                var byteCopied: Long = 0
                val buffer = ByteArray(DEFAULT_BUFFER_SIZE)
                var lastProgress = -1
                while (true) {
                    val bytes = input.read(buffer)
                    if (bytes < 0) break
                    output.write(buffer, 0, bytes)
                    byteCopied += bytes
                    if (length > 0L) {
                        val progress = ((byteCopied * 100L) / length).toInt().coerceIn(0, 100)
                        // 每个百分点最多刷新一次，避免每 8KB 都切主线程并更新通知。
                        if (progress != lastProgress) {
                            lastProgress = progress
                            withContext(Dispatchers.Main.immediate) {
                                DownloadProgressStore.markProgress(progress)
                                showNotification(progress, "下载进度：${progress}%", false)
                            }
                        }
                    }
                }
                output.flush()
            }
        }
    }
    check(file.length() > 0L) { "下载文件为空" }
    return file
}
```

### 16.8 installApk FileProvider

[common/service/DownloadService.kt:186-198](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadService.kt#L186-L198)：

```kotlin
private fun installApk(file: File): Intent {
    val intent = Intent(Intent.ACTION_VIEW)
    val uri = FileProvider.getUriForFile(this, "${this.packageName}.fileprovider", file)
    intent.setDataAndType(uri, "application/vnd.android.package-archive")
    intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
    intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
    return intent
}
```

### 16.9 DownloadProgressStore 全局状态流

[common/service/DownloadProgressStore.kt:7-49](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadProgressStore.kt#L7-L49)：

```kotlin
object DownloadProgressStore {
    private val _state = MutableStateFlow(DownloadProgressState())
    val state: StateFlow<DownloadProgressState> = _state.asStateFlow()

    fun markStarted() { _state.value = DownloadProgressState(status = DownloadStatus.STARTED, progress = 0) }
    fun markProgress(progress: Int) {
        _state.value = DownloadProgressState(status = DownloadStatus.PROGRESS, progress = progress.coerceIn(0, 100))
    }
    fun markSuccess() { _state.value = DownloadProgressState(status = DownloadStatus.SUCCESS, progress = 100) }
    fun markFailed(progress: Int) {
        _state.value = DownloadProgressState(status = DownloadStatus.FAILED, progress = progress.coerceIn(0, 100))
    }
    fun reset() { _state.value = DownloadProgressState() }
}
```

### 16.10 InstallCleanupReceiver 安装后清理

[common/service/InstallCleanupReceiver.kt:8-17](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/InstallCleanupReceiver.kt#L8-L17)：

```kotlin
class InstallCleanupReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent?) {
        if (intent?.action != Intent.ACTION_MY_PACKAGE_REPLACED) return
        runCatching {
            val apkFile = File(context.externalCacheDir, DownloadService.APK_FILE_NAME)
            if (apkFile.exists()) {
                apkFile.delete()
            }
        }
    }
}
```

### 16.11 DomesticUpdateManifest Gitee 清单 DTO

[common/data/remote/dto/DomesticUpdateManifest.kt:8-15](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/remote/dto/DomesticUpdateManifest.kt#L8-L15)：

```kotlin
/**
 * Gitee 国内更新源的稳定清单格式。
 *
 * 清单与 APK 都放在公开仓库中，应用无需登录即可检查和下载更新。
 */
data class DomesticUpdateManifest(
    val versionCode: Long = 0,
    val versionName: String = "",
    val releaseNotes: String = "",
    val apkUrl: String = "",
    val sha256: String = "",
    val publishedAt: String = ""
)
```

### 16.12 GitHubRelease DTO

[common/data/remote/dto/GitHubRelease.kt:9-19](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/remote/dto/GitHubRelease.kt#L9-L19)：

```kotlin
data class GitHubReleaseAsset(
    val name: String = "",
    @SerializedName("browser_download_url") val downloadUrl: String = ""
)

data class GitHubRelease(
    @SerializedName("tag_name") val tagName: String = "",
    @SerializedName("published_at") val publishedAt: String = "",
    val body: String = "",
    val assets: List<GitHubReleaseAsset> = emptyList()
)
```

---

## 17. 主题与换肤系统

### 17.1 SkinColors 设计令牌

[core/theme/AppTheme.kt:23-140](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/theme/AppTheme.kt#L23-L140)：

```kotlin
data class SkinColors(
    val primaryTextColor: Color = DesignColors.TextPrimary,
    val greyTextColor: Color = DesignColors.TextGrey,
    val bgPrimaryColor: Color = DesignColors.BgPrimary,
    // ... 80+ 颜色字段 ...
    val overviewPageBackgroundColor: Color = Color(0xFFF3F3F6),
    val overviewPromptBackgroundColor: Color = Color(0xFFE8EDF8),
    val overviewPromptTextColor: Color = Color(0xFF3D7AF7),
    val campusSkyBlueColor: Color = DesignColors.CampusSkyBlue,
    val campusMintColor: Color = DesignColors.CampusMint,
    // ...
) {
    companion object {
        val Default = SkinColors()
    }
}

val LocalSkinColors = staticCompositionLocalOf { SkinColors.Default }
```

### 17.2 AppSkinTheme 主题入口

[core/theme/AppTheme.kt:375-408](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/theme/AppTheme.kt#L375-L408)：

```kotlin
@Composable
fun AppSkinTheme(
    darkTheme: Boolean? = null,
    content: @Composable () -> Unit
) {
    val selectedThemeMode by ThemeModeManager.mode.collectAsState()
    val systemInDarkTheme = isSystemInDarkTheme()
    val resolvedDarkTheme = darkTheme ?: when (selectedThemeMode) {
        AppThemeMode.LIGHT -> false
        AppThemeMode.DARK -> true
        AppThemeMode.SYSTEM -> systemInDarkTheme
    }
    val useDarkPalette = resolvedDarkTheme

    val currentColors = remember(useDarkPalette) {
        if (useDarkPalette) DarkSkinColors else LightSkinColors
    }

    CompositionLocalProvider(LocalSkinColors provides currentColors) {
        MaterialTheme(
            colorScheme = if (useDarkPalette) PocketDarkColorScheme else PocketLightColorScheme,
            typography = PocketTypography,
            shapes = PocketShapes,
            content = content
        )
    }
}

object AppTheme {
    val colors: SkinColors
        @Composable get() = LocalSkinColors.current
}
```

三种主题模式：LIGHT / DARK / SYSTEM（跟随系统）。

### 17.3 换肤系统

`skin/` 包：
- `SkinManager`：管理皮肤加载、切换、资源替换
- `SkinResourcesHelper` / `SkinFileHelper`：加载外部/Assets 皮肤包
- `SkinCache`：记录已下载皮肤
- `Skin*` 系列自定义 View：在 XML 渲染时支持换肤属性
- `SkinComposeHelper`：让 Compose 也能读取皮肤色值

内置 `skin_default` 和 `skin_dark.apk` 两套皮肤。皮肤包本质是包含同名资源的 apk/zip。

---

## 18. 账号体系与登录绑定

### 18.1 取消自研账号系统

[common/redux/action/UserAction.kt:8-12](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/action/UserAction.kt#L8-L12)：

```kotlin
/**
 * 用户有关的 Action。
 * 目前应用已取消账号系统，仅保留与学号绑定 / 应用更新相关的动作。
 */
sealed class UserAction {
    class QueryIsLastedApk(...) : UserAction()
    class BindingStudentNumber(...) : UserAction()
    class initilaize : UserAction()
    data class WebLoginSuccess(...) : UserAction()
    object RefreshMoocProfileSilently : UserAction()
}
```

### 18.2 唯一登录入口

[core/Route.kt:129-153](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt#L129-L153)：

```kotlin
/**
 * 应用唯一登录入口：跳转到绑定学号页面。
 * @param isSwitchAccount 是否是"切换学号"场景。true 时，绑定成功后会由 BindingUserActivity 负责
 *   清理旧账号的内容缓存；进入绑定页前不会清除任何数据，避免中途放弃导致账号信息丢失。
 */
fun goBindingUser(
    context: Context,
    isSwitchAccount: Boolean = false,
    returnToCaller: Boolean = false
) {
    val intent = Intent(context, BindingUserActivity::class.java)
        .putExtra(BindingUserActivity.EXTRA_IS_SWITCH_ACCOUNT, isSwitchAccount)
        .putExtra(BindingUserActivity.EXTRA_RETURN_TO_CALLER, returnToCaller)
    context.startActivity(intent)
}

/** 强制清栈并跳转到绑定学号页面，用于 token 过期等场景。 */
fun goBindingUserForcibly(context: Context) {
    val intent = Intent(context, BindingUserActivity::class.java)
        .addFlags(Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK)
    context.startActivity(intent)
}
```

### 18.3 BindingStudentNumber SSO + 教务双登录

[common/redux/store/UserStore.kt:145-263](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L145-L263)：

```kotlin
is UserAction.BindingStudentNumber -> {
    if (!bindingInProgress.compareAndSet(false, true)) return
    currentState.uiForLoading = true
    storeScope.launch {
        CsustRetrofitUtils.ClearClient("moocClient")
        CsustRetrofitUtils.ClearClient("EducationClient")
        try {
            val ssoResult = MoocRepository.instance
                .login(action.studentNumber, action.studentPassword)
                .filter { it !is Resource.Loading }
                .first()
            when (ssoResult) {
                is Resource.Success -> {
                    // SSO 登录成功后抓取 MOOC 用户信息并更新头像 / 用户名
                    runCatching {
                        val userResource = MoocRepository.instance.getLoginUser()...
                        if (userResource is Resource.Success) {
                            userResource.data?.let { sso ->
                                if (sso.avatar.isNotBlank()) {
                                    UserInfoManager.userAvatar = sso.avatar
                                }
                                if (sso.userName.isNotBlank()) {
                                    UserInfoManager.account = sso.userName
                                }
                            }
                        }
                    }
                    val eduSuccess = AuthService.login(action.studentNumber, action.studentPassword)
                    if (eduSuccess) {
                        EducationData.studentId = action.studentNumber
                        EducationData.studentPassword = action.studentPassword
                        StudentInfoManager.studentId = action.studentNumber
                        StudentInfoManager.studentPassword = action.studentPassword
                        handler.post { AppEventBus.finishEvent.tryEmit(FinishEvent("bindingUser")) }
                        PlanetApplication.clearSchoolDataCacheAll()
                    } else {
                        // 弹出"学号或密码错误，请重试"
                    }
                }
                is Resource.Error -> { /* 区分"请在手机网页登录一次" 与普通错误 */ }
                else -> { /* 网络或未知错误 */ }
            }
        } finally { bindingInProgress.set(false) }
    }
    // ...
}
```

### 18.4 RefreshMoocProfileSilently 静默迁移

[common/redux/action/UserAction.kt:30-40](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/action/UserAction.kt#L30-L40)：

```kotlin
/**
 * 静默刷新 MOOC 用户资料：用已持久化的学号/密码在后台重新跑一次 SSO 登录，
 * 拿到最新的 userName / avatar 并写回 [UserInfoManager]。
 * 仅用于升级迁移等场景，不弹任何 UI，不发 FinishEvent，不清缓存，失败静默忽略。
 */
object RefreshMoocProfileSilently : UserAction()
```

### 18.5 clearAccountDataNow 退出登录

[core/PlanetApplication.kt:91-112](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L91-L112)：

```kotlin
/**
 * Clears every account-scoped value before the signed-out UI is shown.
 * This is deliberately suspend and does not launch another coroutine: callers must wait
 * until the wipe has finished, otherwise a newly-created screen can restore stale data.
 */
suspend fun clearAccountDataNow() = withContext(Dispatchers.IO) {
    accessToken = ""
    isExpired = true

    listOf(
        "education_cache",
        "content_cache",
        TIME_TABLE_APP_WIDGET,
        "overview_local_cache",
        "mooc_local_cache",
        "MoocCookiejar",
        "stu_info_cache",
        "import_cache"
    ).forEach { cacheId -> MMKV.mmkvWithID(cacheId).clearAll() }

    appContext.getSharedPreferences("physics_experiment", Context.MODE_PRIVATE)
        .edit().clear().commit()
    CoursesDataBase.getDatabase(appContext).courseDao().clearAllCourses()
    runCatching { MoocRepository.instance.clearMoocLocalSession() }
}
```

---

## 19. 路由系统

### 19.1 Route 对象

[core/Route.kt:37-67](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt#L37-L67)：

```kotlin
object Route {
    fun goCampusMap(context: Context) { ... }
    fun goElectronic(context: Context) { ... }
    fun goHome(context: Context) { ... }
    fun goHomeForcibly(context: Context) {
        val intent = Intent(context, MainActivity::class.java)
            .addFlags(Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK)
        context.startActivity(intent)
    }
    // ... 共 20+ 个跳转方法 ...
}
```

### 19.2 WebView 快捷方式

[core/Route.kt:100-104](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt#L100-L104)：

```kotlin
fun goElectricityRecharge(context: Context) = goWebView(context, "https://hxyxh5.csust.edu.cn/plat/shouyeUser")
fun goEvaluation(context: Context) = goWebView(context, "https://zbxt.csust.edu.cn/api/manage/cas/toUrl?type=pc")
fun goNcre(context: Context) = goWebView(context, "https://cjcx.neea.edu.cn/html1/folder/22014/5490-1.htm")
```

### 19.3 MainTabNavigator 底部导航

[core/main/navigation/MainTabNavigator.kt:7-29](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/navigation/MainTabNavigator.kt#L7-L29)：

```kotlin
class MainTabNavigator(
    initialDestination: MainTabDestination = MainTabDestination.Overview
) {
    private var selectedDestinationState by mutableStateOf(initialDestination)

    val currentDestination: MainTabDestination
        get() = selectedDestinationState

    val displayBackStack: List<MainTabDestination>
        get() = listOf(selectedDestinationState)

    fun select(destination: MainTabDestination) {
        if (selectedDestinationState == destination) return
        selectedDestinationState = destination
    }
}
```

### 19.4 MainTabDestination 4 个 Tab

[core/main/navigation/MainTabDestination.kt:8-46](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/navigation/MainTabDestination.kt#L8-L46)：

```kotlin
sealed class MainTabDestination(
    val index: Int,
    @param:DrawableRes @get:DrawableRes val iconResId: Int,
    @param:StringRes @get:StringRes val labelResId: Int
) : NavKey {

    data object Overview : MainTabDestination(index = 0, iconResId = R.drawable.ic_overview, labelResId = R.string.overview)
    data object Feature : MainTabDestination(index = 1, iconResId = R.drawable.nfeature, labelResId = R.string.function)
    data object Profile : MainTabDestination(index = 2, iconResId = R.drawable.nprofile, labelResId = R.string.profile_home)
    data object Mooc : MainTabDestination(index = 3, iconResId = R.drawable.ic_document, labelResId = R.string.pending_homework)
}
```

### 19.5 MainScreen NavDisplay 路由

[core/main/ui/MainScreen.kt:166-217](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/ui/MainScreen.kt#L166-L217)：

```kotlin
NavDisplay(
    backStack = navigator.displayBackStack,
    entryProvider = entryProvider {
        entry<MainTabDestination.Overview> {
            OverviewTabRoute(navigator)
        }
        entry<MainTabDestination.Feature> {
            FeatureScreen(onDestinationSelected = ::openServiceDestination)
        }
        entry<MainTabDestination.Profile> {
            ProfileSettingsRoute()
        }
        entry<MainTabDestination.Mooc> {
            val moocViewModel: MoocViewModel = viewModel()
            MoocScreen(
                moocViewModel = moocViewModel,
                embedded = true,
                onOpenCoursePage = { Route.goMoocCoursePage(context, it) }
            )
        }
    }
)
```

### 19.6 OverviewTabRoute 业务路由

[core/main/ui/MainScreen.kt:404-429](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/ui/MainScreen.kt#L404-L429)：

```kotlin
@Composable
private fun OverviewTabRoute(navigator: MainTabNavigator) {
    val context = LocalContext.current
    val viewModel: OverviewViewModel = viewModel()
    OverviewScreen(
        viewModel = viewModel,
        onBindClick = { Route.goBindingUser(context) },
        onQuickActionClick = { actionId ->
            val shortcut = primaryFunctionShortcuts().firstOrNull { it.id == actionId }
            val serviceShortcut = actionId
                .takeIf { it.startsWith("service:") }
                ?.removePrefix("service:")
                ?.let { serviceId ->
                    portalServiceGroups().flatMap { it.items }.firstOrNull { it.id == serviceId }
                }
            when {
                actionId == "homework" -> navigator.select(MainTabDestination.Mooc)
                serviceShortcut != null -> openFunctionShortcut(context, serviceShortcut.destination)
                shortcut != null -> openFunctionShortcut(context, shortcut.destination)
                actionId == FunctionDestination.ScoreInquiry.name -> Route.goScoreInquiry(context)
                actionId == FunctionDestination.Electronic.name -> Route.goElectronic(context)
            }
        }
    )
}
```

---

## 20. 跨模块数据流转路径汇总

### 20.1 启动 → 首页可见

```text
PlanetApplication.onCreate()                    [PlanetApplication.kt:159-191]
    ├── MMKV 初始化
    ├── ThemeModeManager.initialize()
    ├── migrateLegacyCacheIfNeeded()            [PlanetApplication.kt:249-272]
    └── 异步：initDNS + preRequestIps
        ↓
MainActivity.onCreate()                         [MainActivity.kt:34-76]
    ├── setCustomDensity(412)                   [MainActivity.kt:147-173]
    └── setContent { AppSkinTheme { MainScreen(navigator) } }
        ↓
MainScreen                                       [MainScreen.kt:87-127]
    └── NavDisplay → entry<Overview> { OverviewTabRoute(navigator) }
        ↓
OverviewScreen(viewModel)                       [OverviewScreen.kt:148-190]
    └── viewModel by viewModels()
        ↓
OverviewViewModel.init { load(false) }          [OverviewViewModel.kt:26-49]
    ├── repository.loadLocalState()             [OverviewRepository.kt:54-60]
    └── 条件满足时 repository.refreshState()     [OverviewRepository.kt:62-82]
```

### 20.2 考试高亮流转

```text
ExamArrangeService.getExamArrange(term)
        ↓
examCache.saveExamArrangement(exams)             [ExamArrangementCache.kt:8-36]
        ↓
exams.toUiExams()                                [OverviewRepository.kt:255-263]
        ↓
ExamHighlightSelector.select(exams, now)         [ExamHighlightSelector.kt:8-46]
        ↓
ExamHighlightUiModel
        ↓
OverviewUiState.examHighlight                   [OverviewUiState.kt:31]
        ↓
ExamHighlightCard 渲染
```

### 20.3 公告流转

```text
Gitee config/announcements.json
        ↓
AnnouncementRepository.refresh()                [AnnouncementRepository.kt:42-90]
    ├── If-None-Match / If-Modified-Since 304
    └── 写 MMKV announcement_cache
        ↓
AnnouncementPolicy.visibleAnnouncements(...)     [AnnouncementPolicy.kt:7-32]
        ↓
AnnouncementState(announcements, unreadCount, urgentToPresent)
        ↓
首页铃铛红点 / AnnouncementCenterDialog / UrgentAnnouncementDialog
```

### 20.4 更新流转

```text
MainActivity.onResume()
    └── AppUpdateChecker.check(this)             [AppUpdateChecker.kt:13-32]
        ↓
UserStore.dispatch(QueryIsLastedApk(...))        [UserStore.kt:68-143]
    ├── 30 分钟冷却
    └── updateCheckInProgress 防并发
        ↓
fetchLatestUpdateWithFallback()                  [UserStore.kt:343-371]
    ├── Gitee latest.json (3 次重试)
    └── GitHub releases/latest (3 次重试)
        ↓
isNewerVersion(latest, current)                  [UserStore.kt:439-485]
        ↓
showUpdateDialogIfPossible → UpdateDialog
        ↓
用户确认 → DownloadService.startDownload(apkUrl) [DownloadService.kt:27-83]
    ├── 通知栏进度（每百分点最多刷新一次）       [DownloadService.kt:129-170]
    ├── DownloadProgressStore 状态流             [DownloadProgressStore.kt:7-49]
    └── 下载完成调起系统安装器                    [DownloadService.kt:186-198]
        ↓
InstallCleanupReceiver (ACTION_MY_PACKAGE_REPLACED) [InstallCleanupReceiver.kt:8-17]
    └── 删除 update.apk
```

### 20.5 电费查询流转

```text
ElectricityRepository.query(force)               [ElectricityRepository.kt:32-56]
    ├── 10 分钟缓存检查
    ├── CampusCardHelper.queryElectricity(...)
    ├── processDormAndRoom A/B 栋前缀            [ElectricityRepository.kt:82-92]
    └── OverviewLocalCache.saveElectricitySnapshot(value)
        ↓
OverviewLocalCache                               [OverviewLocalCache.kt:8-46]
    ├── appendElectricityHistory(value)          [OverviewLocalCache.kt:80-124]
    └── 更新 last/prev 字段
        ↓
OverviewRepository.buildState()
    └── buildMetrics(...)                        [OverviewRepository.kt:266-326]
        ↓
OverviewUiState.metrics（电量指标卡）
    ├── 当前剩余
    ├── 估算可用天数（基于 21 天历史中位数）     [OverviewRepository.kt:363-406]
    └── 24 小时趋势图 / 每日用电柱状图
```

### 20.6 MOOC 作业流转

```text
StudentInfoManager.studentId / password          [StudentInfoManager.kt:5-48]
        ↓
MoocDataHelper.fetchMoocCourses(...)             [MoocDataHelper.kt:96-211]
    ├── executeWithRetry 重试 + 重登             [MoocDataHelper.kt:48-69]
    ├── MoocRepository.login (SSO)
    ├── MoocRepository.getCourses
    └── MoocLocalCache.saveCourseItems(items)    [MoocLocalCache.kt:8-40]
        ↓
OverviewViewModel.loadMoocFromCache()
    ├── extractPendingHomeworks(items)            [MoocDataHelper.kt:243-263]
    └── extractPendingTests(items)
        ↓
OverviewUiState.pendingHomeworks / pendingTests
        ↓
首页「待提交作业」卡片 + MOOC tab
```

### 20.7 学号绑定流转

```text
Route.goBindingUser(context)                     [Route.kt:129-153]
        ↓
BindingUserActivity（输入学号 + 密码）
        ↓
UserStore.dispatch(BindingStudentNumber(...))    [UserStore.kt:145-263]
    ├── CsustRetrofitUtils.ClearClient
    ├── MoocRepository.login (SSO)
    ├── MoocRepository.getLoginUser → 更新头像/用户名
    ├── AuthService.login (教务)
    ├── StudentInfoManager.studentId = ...
    └── PlanetApplication.clearSchoolDataCacheAll()
        ↓
AppEventBus.finishEvent.tryEmit(FinishEvent("bindingUser"))
        ↓
BindingUserActivity finish → 返回首页
```

---

## 21. 文件行号速查表

| 文件 | 总行数 | 关键行号 |
|------|--------|----------|
| [PlanetApplication.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt) | 322 | 34-66（常量）、91-112（clearAccountDataNow）、159-191（onCreate）、249-272（缓存迁移） |
| [MainActivity.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt) | 174 | 34-76（onCreate）、101-109（onStart 静默 SSO）、147-173（setCustomDensity） |
| [Route.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt) | 242 | 37-67（Route）、100-104（WebView）、129-153（goBindingUser） |
| [Store.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Store.kt) | 26 | 9-26（Store 基类） |
| [OkHttpHelper.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt) | 477 | 40-120（AuthInterceptor）、124-184（client + HTTPDNS）、252-282（refreshTokenSync）、454-476（preRequest） |
| [MainScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/ui/MainScreen.kt) | 437 | 87-127（MainScreen）、166-217（NavDisplay）、404-429（OverviewTabRoute） |
| [MainTabNavigator.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/navigation/MainTabNavigator.kt) | 30 | 7-29 |
| [MainTabDestination.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/navigation/MainTabDestination.kt) | 47 | 8-46（4 个 Tab） |
| [AppTheme.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/theme/AppTheme.kt) | 408 | 23-140（SkinColors）、375-401（AppSkinTheme）、404-408（AppTheme） |
| [MviViewModel.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/mvi/MviViewModel.kt) | 27 | 9-26 |
| [OverviewRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt) | 555 | 44-83（refreshState）、98-177（buildState 21点切明日）、266-326（buildMetrics）、334-346（preprocessGrades）、363-406（estimateElectricityDays）、445-470（parseWeeks） |
| [ExamHighlightSelector.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/ExamHighlightSelector.kt) | 60 | 8-46（select） |
| [OverviewLocalCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/local/OverviewLocalCache.kt) | 172 | 8-46（缓存键）、80-124（getDailyUsage）、152-171（数据类） |
| [OverviewUiState.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/model/OverviewUiState.kt) | 109 | 7-34 |
| [OverviewScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt) | 2160 | 116-146（常量）、148-190（OverviewScreen 入口）、192-282（私有 OverviewScreen 主体）、284-353（HomeGreetingHeader）、354-606（PortalDashboard）、607-673（LegacyPortalDashboard）、674-783（HomeCourseHero）、784-840（HomeMetricCard）、841-881（HomeMiniTrend）、882-943（HomeStatusCard）、944-1046（ExamHighlightCard 考试高亮卡）、1047-1103（WeeklyStudyOverview）、1104-1172（PortalServiceShortcutCard）、1173-1301（EditableDashboardCard）、1302-1394（AddOverviewCardDialog）、1395-1441（PortalCourseCard）、1442-1551（PortalMetricCard）、1552-1609（PortalStatusCard）、1610-1641（HeaderBlock）、1642-1655（BindPrompt）、1656-末尾（SectionTitle） |
| [AnnouncementCenter.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/AnnouncementCenter.kt) | 422 | 58-100（AnnouncementCenterDialog）、101-156（AnnouncementList）、157-196（AnnouncementTopBar）、197-265（AnnouncementListItem）、266-346（AnnouncementDetail）、347-404（UrgentAnnouncementDialog）、405-430（LevelBadge）、431-422（isHttpUrl） |
| [AnnouncementModels.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementModels.kt) | 59 | 全文：AnnouncementManifestDto/AnnouncementDto/AnnouncementLevel/AnnouncementUiModel/AnnouncementState 数据类定义 |
| [AnnouncementRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementRepository.kt) | 127 | 18-29（URL + 缓存键 + 节流常量）、40（loadCached）、42-90（refresh ETag/304）、92-96（markRead）、98-102（markUrgentPresented）、104-120（stateFromCachedManifest） |
| [AnnouncementPolicy.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementPolicy.kt) | 76 | 10-32（visibleAnnouncements）、34-65（toUiModelOrNull 版本/时间窗口过滤）、67-68（parseTime）、70-75（level priority） |
| [OverviewViewModel.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt) | 235 | 26（OverviewViewModel 类）、对应 StateFlow 暴露与 refresh 调度逻辑 |
| [AppUpdateChecker.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/update/AppUpdateChecker.kt) | 29 | 全文：update 检查入口（实际逻辑在 UserStore.QueryIsLastedApk） |
| [ReleaseNotesFormatter.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/update/ReleaseNotesFormatter.kt) | 40 | 全文：将 GitHub/Gitee release body 格式化为更新公告文本 |
| [DownloadService.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadService.kt) | 203 | 27（DownloadService Service 类）、onStartCommand 下载、Notification 进度、下载完成触发安装 |
| [DownloadProgressStore.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/DownloadProgressStore.kt) | 40 | 全文：MMKV 持久化下载进度（url/progress/total/savedPath） |
| [InstallCleanupReceiver.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/service/InstallCleanupReceiver.kt) | 16 | 全文：监听 PACKAGE_REPLACED/ACTION_PACKAGE_ADDED 清理旧 APK |
| [UserAction.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/action/UserAction.kt) | 34 | 8-12（sealed class 注释）、QueryIsLastedApk/initialize/RefreshMoocProfileSilently/BindUser 等 Action |
| [UserStore.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt) | 458 | 41（UserStore 类）、更新检查 Gitee→GitHub 降级、学号绑定 SSO+教务双登录、QueryIsLastedApk 流程 |
| [StudentInfoManager.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/local/mmkv/StudentInfoManager.kt) | 39 | 全文：学号/教务密码 MMKV 持久化（stu_info_cache 分区） |
| [UserInfoManager.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/local/mmkv/UserInfoManager.kt) | 75 | 全文：MOOC 用户名/头像/ID/邮箱 MMKV 持久化（import_cache 分区） |
| [CommonInfo.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/cache/CommonInfo.kt) | 261 | 23（CommonInfo object）、学期推断月份兜底、周次计算、startTime |
| [DomesticUpdateManifest.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/remote/dto/DomesticUpdateManifest.kt) | 14 | 全文：Gitee 国内更新清单 DTO（versionCode/versionName/downloadUrl/updateMessage） |
| [GitHubRelease.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/data/remote/dto/GitHubRelease.kt) | 16 | 全文：GitHub Release DTO（tag_name/body/assets.browser_download_url） |
| [ElectricityRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt) | 97 | 7-16（ElectricityBinding 持久化）、20-30（shouldAutoRefresh）、32-56（query 网络拉取）、58-66（saveBinding）、82-92（processDormAndRoom A/B 栋前缀补全）、106-113（常量） |
| [ElectronicScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/compose_ui/ElectronicScreen.kt) | 1234 | 111-337（ElectronicScreen 入口）、338-357（CampusCard）、358-365（CampusDivider）、366-415（SelectorRow）、416-515（ElectricityUsageCard）、516-551（RealtimeElectricityContent）、552-599（RealtimeElectricityChart）、610-680（ElectricityLineChart）、681-804（ElectricityResult）、805-815（processDormAndRoom）、830-929（ReminderDialog）、930-995（BindingSummaryButton 寝室绑定按钮）、996-1184（DormBindingSheet 房间号滑动选择）、1185-1189（shouldClearRoomForBindingChange） |
| [Grade.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/entity/Grade.kt) | 46 | 全文：成绩实体类（课程名/学分/绩点/学期/重修补考标记） |
| [TimeTableMySubject.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/entity/TimeTableMySubject.kt) | 54 | 全文：课表实体类（课程名/周次/节次/教室/老师） |
| [ExamArrangementCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ExamArrangementCache.kt) | 38 | 全文：考试安排 MMKV 缓存（content_cache 分区） |
| [ScoreCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ScoreCache.kt) | 74 | 全文：成绩 MMKV 缓存（content_cache 分区，按学期分键） |
| [CoursesDataBase.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/room/database/CoursesDataBase.kt) | 28 | 全文：Room v10 数据库 + courseDao + clearAllCourses |
| [SemesterCalendarRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/calendar/data/repository/SemesterCalendarRepository.kt) | 158 | 校历数据拉取与缓存（学期开始日期、教学周列表） |
| [MoocDataHelper.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt) | 311 | 28（MoocDataHelper object）、网络教学平台登录重试+重登、待作业/课程列表提取 |
| [MoocLocalCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/local/MoocLocalCache.kt) | 33 | 全文：MOOC 待作业/课程列表 MMKV 缓存（mooc_local_cache 分区） |
| [build.gradle.kts](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts) | 245 | 42-43（versionCode=54/versionName=2.0.32）、AndroidManifest、Compose、高德 3D 地图、HTTPDNS、Bugly、csust_spider aar 依赖配置 |

---

## 22. 当前使用状态总览

> 本节基于静态调用链分析，明确标注每个模块/路由/组件在 v2.0.32（versionCode=54）当前版本中**是否真正在主流程中被使用**。区分"当前使用 / 部分使用 / 弃用"三档。

### 22.1 主屏 4 个 Tab（全部当前使用）

[MainTabDestination.kt:8-46](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/navigation/MainTabDestination.kt#L8-L46) + [MainScreen.kt:185-213](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/ui/MainScreen.kt#L185-L213)：

| index | Tab | 当前状态 |
|-------|-----|---------|
| 0 | Overview（概览） | ✅ 当前使用 |
| 1 | Feature（功能） | ✅ 当前使用 |
| 2 | Profile（我的） | ✅ 当前使用 |
| 3 | Mooc（待提交作业） | ✅ 当前使用 |

### 22.2 首页 PortalDashboard vs LegacyPortalDashboard

[OverviewScreen.kt:122](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L122) 顶部开关 `EnableHomeCardEditor = false`（注释明确写"Deferred for a later release. Keep the implementation locally without exposing it in v2.0.29."）。

[OverviewScreen.kt:364-373](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L364-L373) 中 `PortalDashboard` 立即判定 `if (!EnableHomeCardEditor)` 走 `LegacyPortalDashboard` 并 return。

| 组件 | 当前状态 |
|------|---------|
| `LegacyPortalDashboard` ([OverviewScreen.kt:607-672](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L607-L672)) | ✅ 当前使用（用户实际看到的就是这个） |
| `PortalDashboard` + `EditableDashboardCard` + `AddOverviewCardDialog` | ⚠️ 死代码（已实现但被开关关闭） |

### 22.3 UserStore 5 个 Action（全部当前使用）

[UserStore.kt:61-321](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L61-L321) + [UserAction.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/action/UserAction.kt)：

| Action | 调用点 | 当前状态 |
|--------|--------|---------|
| `initilaize` | [MainActivity.kt:40](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt#L40)、BindingUserActivity | ✅ 当前使用（注：源码拼写为 `initilaize` 是 typo，但仍活跃） |
| `QueryIsLastedApk` | [AppUpdateChecker.kt:24-31](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/update/AppUpdateChecker.kt#L24-L31) | ✅ 当前使用 |
| `BindingStudentNumber` | BindingUserActivity 绑定按钮 | ✅ 当前使用 |
| `WebLoginSuccess` | BindingUserActivity WebView 登录回调 | ✅ 当前使用 |
| `RefreshMoocProfileSilently` | [MainActivity.kt:108](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt#L108) | ✅ 当前使用 |

### 22.4 网络后端域名

[PlanetApplication.kt:57-66](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L57-L66)：

| 域名 | 当前状态 |
|------|---------|
| `UserIp = "https://user.csust.creamaker.cn"` | ✅ 当前使用（预热） |
| `ToolIp = "https://web.csust.creamaker.cn"` | ✅ 当前使用（预热） |
| `FreshNewsIp = "http://freshnews.csust.creamaker.cn"` | ❌ 弃用（源码已注释） |

### 22.5 各功能模块当前状态

| 模块 | 入口 | 当前状态 | 证据 |
|------|------|---------|------|
| 首页概览（Overview） | MainScreen Overview Tab | ✅ 当前使用 | [OverviewScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt) |
| 功能页（Feature） | MainScreen Feature Tab | ✅ 当前使用 | `FeatureScreen` Composable |
| 个人中心（Profile） | MainScreen Profile Tab | ✅ 当前使用 | `ProfileSettingsRoute` |
| MOOC 待作业 | MainScreen Mooc Tab | ✅ 当前使用 | [MoocViewModel](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/viewmodel/MoocViewModel.kt) + [MoocDataHelper.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/mooc/data/MoocDataHelper.kt) |
| 电费查询 | `Route.goElectronic` → ElectronicActivity → [ElectronicScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/compose_ui/ElectronicScreen.kt) | ✅ 当前使用（仓库内不存在 `ElectricScreen`） | [ElectricityRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/repository/ElectricityRepository.kt) |
| 课表 | `Route.goTimetable` → TimeTableActivity → `TimeTableComposeScreen` | ✅ 当前使用（Compose 实现，无 Fragment 版本） | [TimeTableActivity.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/timetable/ui/TimeTableActivity.kt) |
| 成绩查询 | `Route.goScoreInquiry` → ScoreInquiryActivity | ✅ 当前使用 | [ScoreCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ScoreCache.kt) 9 处调用 |
| 成绩分析 | `Route.goGradeAnalysis` → GradeAnalysisScreen | ✅ 当前使用 | [GradeAnalysisScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/ui/compose/GradeAnalysisScreen.kt) |
| 考试安排 | `Route.goExamArrangement` | ✅ 当前使用 | [ExamArrangementCache.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/data/local/mmkv/ExamArrangementCache.kt) |
| 校历 | `Route.goCalendar` | ✅ 当前使用 | [SemesterCalendarRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/calendar/data/repository/SemesterCalendarRepository.kt) |
| 校园地图 | `Route.goCampusMap` → [CampusMapActivity.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/ui/CampusMapActivity.kt) | ✅ 当前使用（高德 3D 地图唯一场景） | [build.gradle.kts:256-258](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L256-L258) |
| 物理实验 | `Route.goPhysicsSchedule / goPhysicsGrade` | ✅ 当前使用 | PhysicsExperimentActivity |
| 记账本 | `Route.goAccountBook / goAddSomethingAccount / goFixSomethingAccount` | ✅ 当前使用 | AccountBookActivity + AccountBookDatabase |
| 公告系统 | OverviewScreen `LaunchedEffect` + AnnouncementCenterDialog | ✅ 当前使用（已上线） | [AnnouncementRepository.kt:18-19](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/announcement/AnnouncementRepository.kt#L18-L19) URL 指向 Gitee |
| 更新检查 | `MainActivity.onResume` + "我的"页手动检查 | ✅ 当前使用（Gitee 主源 + GitHub 降级） | [UserStore.kt:343-371](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/common/redux/store/UserStore.kt#L343-L371) |
| 学号绑定 | `Route.goBindingUser` → BindingUserActivity | ✅ 当前使用（9 处调用点） | [Route.kt:129-153](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt#L129-L153) |
| 新鲜事（freshNews） | `Route.goUserHomeActivity / goPublishFreshNews` | ❌ **弃用**（整模块无运行时入口） | freshNews/ 目录 41 个 kt 文件，Route 方法全仓无调用 |

### 22.6 主题与换肤系统（部分使用）

[PlanetApplication.kt:188-189, 211-216, 273-276](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L188-L189)：

| 子系统 | 当前状态 | 说明 |
|--------|---------|------|
| `AppSkinTheme`（Compose 主题） | ✅ 当前使用 | `MainScreen` / `MainActivity` / `TimeTableActivity` / `FeatureFragment` / `ProfileSettingsRoute` 均包裹 |
| `SkinManager.setSkin` | ✅ 当前使用 | 启动时从 `SkinCache` 恢复上次皮肤 |
| `SkinXxx` 自定义 View（SkinTextView/SkinImageView/SkinLinearLayout...） | ⚠️ 部分使用 | 主要在老 View 体系页面，Compose 页面不依赖 |
| assets 打包的皮肤 apk | ⚠️ 仅 `skin_dark.apk` | `skin_default` 是 sentinel 字符串而非实际文件 |
| `Route.goSkinSecletion` 皮肤选择页 | ✅ 当前使用 | 设置→"主题设置" |

### 22.7 高德地图 SDK（当前使用，唯一场景）

| 维度 | 状态 |
|------|------|
| 依赖 `com.amap.api:3dmap-location-search:11.2.000` | ✅ [build.gradle.kts:256-258](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/build.gradle.kts#L256-L258) |
| `initAMapPrivacy` 隐私合规 | ✅ [PlanetApplication.kt:200-209](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L200-L209) |
| 唯一使用场景 | [CampusMapActivity.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/feature/common/ui/CampusMapActivity.kt) 校园地图（MapView + 定位 + 多边形建筑 + URI 步行导航） |
| 入口路径 | 首页 → 功能页 → 校园工具 → 校园地图（`Route.goCampusMap`） |
| `app/libs/Lite3DMap.jar` | ❌ 早期 WebView 版残留，已被原生 SDK 替代 |

### 22.8 Route.kt 跳转方法当前状态

[Route.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt) 共 35 个跳转方法，**仅 3 个弃用**：

| 方法 | 当前状态 |
|------|---------|
| `goUserHomeActivity`（2 个重载） | ❌ 弃用（freshNews 下线遗留） |
| `goPublishFreshNews` | ❌ 弃用（freshNews 下线遗留） |
| 其余 32 个方法（`goCampusMap` / `goElectronic` / `goTimetable` / `goScoreInquiry` / `goBindingUser` / `goMooc*` / `goCalendar` / `goPhysics*` / `goAccountBook*` / `goCampusMap` 等） | ✅ 当前使用 |

### 22.9 死代码 / legacy 清单

以下代码编译进 APK 但运行时无入口，**建议清理或归档**：

1. **`freshNews/` 整模块**（41 个 kt 文件）：`FreshNewsScreen`、`UserHomeActivity`、`PublishFreshNewsActivity`、`NewsFragment`、`CommentsActivity` 等无运行时入口
2. **`Route.goUserHomeActivity`、`Route.goPublishFreshNews`**：[Route.kt:44-53, 155-158](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt#L44-L53)
3. **`PortalDashboard` 内可拖拽编辑卡片逻辑**：[OverviewScreen.kt:355-605](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt#L355-L605)（被 `EnableHomeCardEditor=false` 短路）
4. **legacy Fragment 类**：`FeatureFragment`、`OverviewFragment`、`IMFragment`、`NewsFragment`（文件内 Composable 仍活跃，但 Fragment 类自身无引用）
5. **`FunctionSection.kt`**：整个文件仅在自身 Preview 中调用，`FeatureScreen` 已改用 `ServiceHierarchyGroup`
6. **`FunctionCatalog.kt:109-116` 的 `secondaryFunctionShortcuts()`**：未被外部调用
7. **`app/libs/Lite3DMap.jar`**：早期 WebView 版地图残留
8. **`PlanetApplication.FreshNewsIp`**：[PlanetApplication.kt:59, 65](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt#L59)（已注释）
9. **`AndroidManifest.xml` 中 freshNews 相关 Activity 注册**：`UserHomeActivity`、`PublishFreshNewsActivity`、`CommentsActivity` 等死配置

### 22.10 状态图例

- ✅ **当前使用**：在主流程中有调用方，用户可触达
- ⚠️ **部分使用**：组件本身仍活跃，但仅服务于老代码路径或被开关关闭
- ❌ **弃用**：编译进 APK 但运行时无入口，属 legacy 死代码

---

## 23. 阅读建议

### 23.1 入门顺序

1. 先读 [PlanetApplication.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/PlanetApplication.kt) + [MainActivity.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/MainActivity.kt)，理解启动链路
2. 再读 [Route.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/Route.kt) + [MainScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/main/ui/MainScreen.kt)，掌握页面跳转
3. 接着读 [OkHttpHelper.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/core/network/OkHttpHelper.kt)，理解网络层与 Token 刷新
4. 然后读 [OverviewRepository.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/data/OverviewRepository.kt) + [OverviewViewModel.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/viewmodel/OverviewViewModel.kt) + [OverviewScreen.kt](file:///c:/Users/Ivan/Documents/Codex/2026-07-16/creamakers-csustdataget-https-github-com-creamakers/work/CSUSTPocket/app/src/main/java/com/creamaker/changli_planet_app/overview/ui/compose/OverviewScreen.kt)，吃透首页主流程
5. 最后按模块读：公告 / 电费 / 成绩 / 课表 / MOOC / 更新

### 23.2 关键调用链速记

- **启动**：`PlanetApplication.onCreate` → `MainActivity.onCreate(setCustomDensity)` → `MainScreen` → `OverviewScreen` → `OverviewViewModel.refresh` → `OverviewRepository.refreshState`
- **Token 刷新**：`OkHttpHelper.AuthInterceptor.intercept` → 401 → `refreshTokenSync` → 重新发起原请求
- **学号绑定**：`Route.goBindingUser` → `BindingUserActivity` → `UserStore.BindUser` → `AuthService.login` + SSO → 写入 `StudentInfoManager` + `UserInfoManager` → `AppEventBus.finishEvent` → 返回首页
- **更新检查**：`MainActivity.onResume` → `AppUpdateChecker.check` → `UserStore.QueryIsLastedApk` → Gitee 失败降级 GitHub → `DownloadService` → `InstallCleanupReceiver`
- **公告**：`OverviewViewModel` → `AnnouncementRepository.refresh`（节流 60s + ETag/304）→ `AnnouncementPolicy.visibleAnnouncements`（版本范围+时间窗口）→ `AnnouncementCenterDialog` / `UrgentAnnouncementDialog`
- **考试高亮**：`OverviewRepository.buildState` → `ExamHighlightSelector.select(exams, now)` → 4 级优先级 → `OverviewUiState.examHighlight` → `OverviewScreen.ExamHighlightCard`

### 23.3 文档维护

- 新增文件时请在第 21 节速查表追加条目，包含总行数与关键行号
- 修改关键逻辑时请同步更新对应章节的代码引用
- 所有文件链接均使用 `file:///` 绝对路径，可直接在 IDE 中点击跳转

---

> 文档结束。