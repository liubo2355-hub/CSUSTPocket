# 掌上长理 v2.0.42

本版本在 `v2.0.41` 许可证与项目来源整改基础上，进一步落实 CreaMakers 后端服务使用约束：

- 校园地图数据成功后缓存 24 小时。
- 合并同类并发请求，避免重复访问。
- 校历主动刷新设置 30 秒最短间隔。
- 请求失败后设置冷却窗口。
- 关闭 HTTP 客户端透明自动重试。
- 请求携带可审计的客户端名称与版本标识。
- 后端不可用时优先使用已有本地缓存。
