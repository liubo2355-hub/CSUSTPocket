<?xml version="1.0" encoding="utf-8"?>
<com.creamaker.changli_planet_app.widget.view.MaxHeightLinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/maxHeightLayout"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:background="@drawable/dialog_background"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="15dp">

        <TextView
            android:id="@+id/select_userprofile_title"
            android:layout_gravity="center"
            android:textStyle="bold"
            android:layout_width="0dp"
            android:layout_weight="6"
            android:gravity="center"
            android:textSize="20sp"
            android:text="选择"
            android:textColor="@color/color_text_primary"
            android:layout_height="match_parent" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_divider" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/selectRecyclerUserProfile"
        android:background="@color/color_bg_recycler_view"
        android:paddingBottom="7dp"
        android:paddingTop="6dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</com.creamaker.changli_planet_app.widget.view.MaxHeightLinearLayout>